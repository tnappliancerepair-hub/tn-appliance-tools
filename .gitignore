# Xano workspace pull - contains hardcoded secrets (Twilio creds, swagger tokens, etc.)
# Never commit. This is local source-of-truth for Claude Code working with Xano backend.
xano-workspace/
/xano-workspace
./xano-workspace

# Local debugging / scratch / build artifacts.
.netlify/
*.tmp.json
*.bak
.claude/settings.local.json
.tmp_smoke/
.smoke_check.js
