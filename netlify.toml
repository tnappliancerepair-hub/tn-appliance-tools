[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Scheduled function: AHS dispatch email poller (every 15 minutes).
# Reads tnappliancerepair@gmail.com via Gmail API, extracts dispatch.xml
# attachments from Frontdoor emails, POSTs to Xano ahs_email_intake.
# See docs/gmail-oauth-setup.md for one-time OAuth setup.
#
# Schedule was DISABLED 2026-05-12 (commit c707df4) pending clean-data
# investigation. Earlier diagnostic suggested ~3 parallel invocations per
# cron boundary, but the 7:01 PM 2026-05-11 parallel pair (request IDs
# cd08e69f + a46731eb) is now suspected to have been a manual Run-now
# double-click rather than automated retry behavior. RE-ENABLED 2026-05-12
# morning to observe one cron boundary with ZERO manual interference.
[functions."ahs-gmail-poller"]
  schedule = "*/15 * * * *"
