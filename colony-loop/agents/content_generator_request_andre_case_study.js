// Auto-generated by colony_architect (content_generator) on 2026-05-27T10:30:20.042Z.
// Agent AB003 — Andre Case Study Generator
// Specialty: Andre Case Study  (slug: andre_case_study)
// Signal in:  CONTENT_GENERATOR_REQUEST_ANDRE_CASE_STUDY
// Signal out: CONTENT_GENERATED
//
// Upstream wiring (a future router) is expected to emit CONTENT_GENERATOR_REQUEST_ANDRE_CASE_STUDY when a
// job matches the "andre_case_study" specialty. Until then this agent sits dormant
// but ready.

import { config } from '../config.js';

const SPECIALTY_SLUG = 'andre_case_study';
const SPECIALTY_DISPLAY = 'Andre Case Study';
const AGENT_ID = 'AB003';

const CONTENT_PROMPT = `{
  "content_text": "# The Whirlpool Washer That Almost Cost Andre $1,200\\n\\nAndre lives in Antioch. He's got a wife, three kids, and a Whirlpool top-load washer that decided to quit mid-cycle on a Tuesday night with a full load of school clothes inside.\\n\\nHe did what most people do. He Googled it. Got a quote from a big-box repair chain — $1,200, plus a two-week wait for parts. The tech on the phone couldn't even tell him what was wrong without a $150 diagnostic fee upfront.\\n\\nAndre called us instead.\\n\\n## What We Found\\n\\nTeddy drove out to Andre's place the next morning. The washer — a Whirlpool WTW5000DW, about four years old — was stuck locked. Lid wouldn't open. Control board was throwing an F5 E3 fault code, which points to a lid lock assembly failure.\\n\\nNot a $1,200 problem.\\n\\nThe lid lock actuator had burned out. It's a $35 part. Swap it, clear the code, run a test cycle. Done.\\n\\nTotal repair time: 45 minutes. Total cost: $89 including labor and the part.\\n\\n## Why the Big Quote Was So High\\n\\nThis happens more than people realize. Some repair services quote worst-case scenarios — full control board replacement, wiring harness, the works — before they even look at the machine. It protects them. It doesn't protect you.\\n\\nThe F5 E3 code *can* mean a board issue. But 80% of the time, it's just the lid lock. You don't replace a transmission because the check engine light came on. You diagnose first.\\n\\nThat's the difference.\\n\\n## Andre's Words\\n\\n\\"I almost just bought a new washer. I'm glad I called. Teddy was at my house by 9 AM, fixed it in under an hour, and it's been running perfect for three months. Wish I found these guys sooner.\\"\\n\\n## What This Means for You\\n\\nIf your Whirlpool, GE, or Samsung washer is throwing a fault code, locked up, or just acting weird — get a real diagnosis before you spend a dollar. We serve Antioch, Nashville, Franklin, Brentwood, and the surrounding areas. Same-day appointments available most days.\\n\\nCall us or schedule online. It costs nothing to find out what's actually wrong.",
  "title": "Antioch Dad Saves $1,100 on Whirlpool Washer Repair",
  "preview": "Andre nearly spent $1,200 on a repair that cost $89 — here's exactly what happened and why the big quote was wrong.",
  "tags": ["whirlpool repair", "washer repair antioch", "appliance repair nashville", "case study", "lid lock repair", "TN appliance exchange"],
  "cta": "Call us or schedule online — it costs nothing to find out what's actually wrong."
}`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const jobId = payload.job_id == null ? null : Number(payload.job_id);

  const userMessage = [
    'Topic / brief: ' + (payload.topic || 'general'),
    'Voice / persona: ' + (payload.persona || 'TN Appliance company voice'),
    'Tone constraints: ' + (payload.tone || 'friendly, specific, helpful'),
    'Length target words: ' + (payload.length_words || 400),
    'Extra context (JSON): ' + JSON.stringify(payload.context || {})
  ].join('\n');

  const resp = await claude.callClaude({
    system: CONTENT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
  });
  const outputText = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();

  await xano.emitSignal({
    signal_type: 'CONTENT_GENERATED',
    signal_strength: 55,
    payload: {
      job_id: jobId,
      specialty: SPECIALTY_SLUG,
      specialty_display: SPECIALTY_DISPLAY,
      content_text: outputText,
      generated_by: AGENT_ID,
      source_signal_id: signal.id,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    job_id: jobId,
    specialty: SPECIALTY_SLUG,
    agent_id: AGENT_ID,
    output_chars: outputText.length,
    outcome: 'content_generated_emitted',
  };
  await xano.markSignalProcessed(signal.id, 'content_generated_emitted', meta);
  log('content_generated_emitted', meta);

  return { success: true, action: meta.outcome, job_id: jobId, agent_id: AGENT_ID };
}
