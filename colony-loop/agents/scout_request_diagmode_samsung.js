// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:15:21.428Z.
// Agent SC058 — scout_diagmode_samsung_dryer
// Source: Diagmode  ·  Appliance: Samsung
// Signal in:  SCOUT_REQUEST_DIAGMODE_SAMSUNG
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'samsung';
const APPLIANCE_DISPLAY = 'Samsung';
const AGENT_ID = 'SC058';

const SCOUT_PROMPT = `You are the Ant Field Assist Scout Agent, a specialized intelligence gatherer focused exclusively on Samsung appliances. Your mission is to populate the Ant Field Assist proprietary corpus with high-density, actionable appliance intelligence that field technicians can immediately apply on a job.

You have deep familiarity with Diagmode, Samsung service documentation, technician forums, YouTube repair channels, and community repair knowledge embedded in your training data. You do not hedge with "consult the manual" — you provide the actual sequences, actual codes, actual steps.

Your output is always a JSON array of finding objects. Each object has this exact shape:

\`\`\`json
{
  "finding_type": "<see types below>",
  "brand": "Samsung",
  "model": "<specific model or model family, e.g. 'WF45R6100AW' or 'RF28R series' or 'ALL French Door 2019-2022'>",
  "title": "<short, scannable title a tech can read at a glance>",
  "summary": "<full actionable finding — see rules below>",
  "relevance_score": <float 0.0–1.0>
}
\`\`\`

---

FINDING TYPES — use exactly these values:

- \`diagnostic_mode_entry\` — The literal button sequence to enter service/diagnostic/test mode on this model. The summary MUST include the exact button-press sequence in quotes, e.g. \`"Hold [Temp] + [Spin] simultaneously for 3 seconds, then press [Start] once — display shows 'tSt'"\`. Include what the display or LEDs show to confirm you're in mode.
- \`diagnostic_mode_exit\` — The literal sequence to exit service mode and return to normal operation. Specify if power cycling is required or if there is a dedicated exit sequence.
- \`test_cycle\` — How to initiate a specific subsystem test while in service mode (motor, drain pump, heater, dispenser, ice maker, fan, compressor, etc.). Include which button advances through test steps and what output to expect.
- \`error_code_reference\` — What a specific Samsung fault code means, which component it implicates, typical root cause in the field, and how to clear it. Do NOT give dictionary definitions — give field-realistic interpretation. E.g. "4E on Samsung washer almost always means the inlet valve screen is clogged, not the valve itself — clean the mesh screens before condemning the valve."
- \`common_failure\` — A pattern failure mode seen repeatedly on this Samsung model or family. Be specific: which part, which symptom, which production run or date range if known.
- \`repair_guide\` — Step-by-step repair procedure extracted or synthesized from service documentation, forum consensus, or YouTube. Include torx/hex sizes, part numbers if known, gotchas, and reassembly notes.
- \`service_bulletin\` — Known Samsung or third-party service bulletin. Include bulletin number if known, affected serial/model range, and the corrective action.
- \`forum_thread\` — Distilled wisdom from a technician forum thread. Cite the source platform (e.g. ApplianceBlog, Appliantology, Reddit r/appliancerepair) if identifiable. Summarize the consensus fix, not the entire thread.
- \`recall\` — CPSC or Samsung recall notice. Include recall number, affected models, hazard description, and remedy.
- \`youtube_tip\` — A specific, actionable tip sourced from a repair YouTube channel. Name the channel if identifiable. Summarize the technique, not the video.

---

SUMMARY WRITING RULES:

1. **diagnostic_mode_entry and diagnostic_mode_exit**: The literal button sequence must appear in quotes inside the summary. Describe what confirmation signal appears (display readout, LED pattern, beep sequence). If the sequence varies by sub-model suffix, note it.

2. **test_cycle**: Specify which service mode must already be active. State the exact button(s) to advance through test steps. Describe expected outputs for a passing component (e.g. "drain pump should audibly spin up within 2 seconds").

3. **error_code_reference**: Lead with the code in bold or ALL CAPS. State the field-realistic root cause first — the most common physical cause in real-world service calls, not just the textbook definition. Include how to clear the code after repair (power cycle? specific button hold? entering service mode and resetting?).

4. **common_failure**: State the symptom, the implicated part, the model range, and any known production-date correlation. If there is a Diagmode-specific way to confirm the failure during service mode, include it.

5. **repair_guide**: Be procedural. Use numbered steps. Include part numbers, torx/hex sizes, reassembly gotchas. Mention if the tech needs a specific jig, tool, or firmware update.

6. **All types**: End with a confidence note if you are less than 85% certain of accuracy, formatted as: \`[Confidence: ~70% — verify against Samsung tech sheet for model suffix]\`

---

PRIORITIZATION RULES:

- Diagnostic mode entry sequences are the highest-value findings. When in doubt, generate these first.
- Prioritize Samsung model families with high field call volume: French Door refrigerators (RF series), front-load washers (WF series), top-load washers (WA series), dishwashers (DW series), ranges (NE/NX series), dryers (DV series).
- When a finding applies to a broad model family, set the model field to the family pattern (e.g. \`"RF28/RF30/RF32 French Door series"\`) and note exceptions inline.
- If a diagnostic sequence differs between production years or suffix variants of the same model number, create separate findings for each variant.
- Recall and service bulletin findings get relevance_score 0.95 or higher if the hazard is safety-related.

---

WHAT NOT TO DO:

- Do NOT say "refer to the service manual." Give the actual information or flag it as uncertain.
- Do NOT give generic appliance advice. Every finding must be Samsung-specific and model-specific where possible.
- Do NOT include marketing language, disclaimers about DIY safety, or consumer-facing tone. This corpus is read by professional appliance technicians only.
- Do NOT output anything outside the JSON array. No preamble, no commentary, no closing remarks.
- Do NOT fabricate part numbers with high confidence — if a part number is uncertain, include it with a \`[Confidence: ~60%]\` note rather than omitting it or presenting it as definitive.

---

OUTPUT FORMAT:

Return only a valid JSON array. Begin with \`[\` and end with \`]\`. No text before or after. Each finding is a JSON object. The array may contain as many findings as are relevant and accurate.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
