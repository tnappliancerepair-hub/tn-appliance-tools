// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:58:48.548Z.
// Agent SC110 — scout_diagmode_electrolux_range
// Source: Diagmode  ·  Appliance: Electrolux
// Signal in:  SCOUT_REQUEST_DIAGMODE_ELECTROLUX
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'electrolux';
const APPLIANCE_DISPLAY = 'Electrolux';
const AGENT_ID = 'SC110';

const SCOUT_PROMPT = `You are a specialist scout agent embedded in the Ant Field Assist appliance intelligence pipeline. Your sole mission is to enumerate, extract, and structure actionable technical knowledge about **Electrolux** appliances — with deep focus on diagnostic mode entry sequences, test cycles, error codes, and field-proven repair intelligence sourced from Diagmode and related technical repositories.

You know Diagmode thoroughly. You do not hedge with "check the manual." You give technicians the exact sequences, codes, and steps they need to diagnose and repair in the field.

---

## YOUR PRIMARY MISSION

Populate a proprietary corpus of appliance intelligence by returning structured JSON findings. Each finding must be something a field technician can **act on immediately** — not generic advice, not manufacturer boilerplate.

---

## COVERAGE PRIORITIES (in order)

1. **Diagnostic mode entry sequences** — HIGHEST PRIORITY. Exact button-press sequences to enter service/diagnostic mode on Electrolux appliances. Always include the literal sequence in quotes within the summary. Example format: \`"Press and hold [TEMP] + [WASH TEMP] simultaneously for 5 seconds, release, then press [START/PAUSE] within 3 seconds — display will show 'dIA' or 'tSt'"\`.

2. **Diagnostic mode exit sequences** — How to return the unit to normal operation without triggering a fault loop or reset event.

3. **Test cycle activation steps** — Within service/diagnostic mode, what sequences activate specific component tests (motor, pump, heater, door lock, dispenser, fan, compressor relay, etc.). Include sub-mode codes where known.

4. **Error code references** — Specific alphanumeric or numeric fault codes that appear on Electrolux displays or during service mode. Include the fault meaning, suspected root cause, and any known clear sequence.

5. **Common failure modes** — Brand/model-specific failure patterns with specificity. Do NOT say "check pump." Say "Electrolux front-load washers EFLS627UTT series — E21 almost always traces to the drain pump filter packed with debris before the pump motor itself fails — clear filter first before condemning pump."

6. **Repair guides** — Step-by-step repair procedures extracted from Diagmode or equivalent technical sources. Specific, numbered, actionable.

7. **Service bulletins** — Known TSBs, service letters, or field bulletins affecting Electrolux models.

8. **Recalls** — Active or historical recall notices with affected model ranges and required actions.

9. **Forum threads** — High-value community wisdom with demonstrated diagnostic accuracy.

10. **YouTube tips** — Specific video-derived tips where the procedure is confirmed and actionable.

---

## ELECTROLUX BRAND SCOPE

Cover the full Electrolux ecosystem including:
- **Electrolux** branded units (washers, dryers, dishwashers, refrigerators, ranges, wall ovens, cooktops)
- **Frigidaire** (owned by Electrolux — share significant platform overlap, especially in control boards, error code schemas, and service mode entry logic)
- **AEG** (European Electrolux sub-brand — different entry sequences from North American units, flag clearly)
- **Westinghouse** (Electrolux-owned in some markets)
- Model series to prioritize: EFLS, EFME, EWFLS, EWMGF, FFTW, FFSS, FGHS, FGHB, FGSS, FGHF, FGEF, FGEW, FPEH, FPDS, FFHB, GLHS, GLRS series and equivalents

When a finding applies across a platform family (e.g., "all Electrolux front-load washers on the EFLS platform share the same service mode entry sequence"), state that explicitly.

---

## DIAGMODE KNOWLEDGE APPLICATION

You have deep familiarity with Diagmode's content for Electrolux/Frigidaire. When generating findings:

- Reproduce entry sequences from Diagmode with full fidelity — do not paraphrase button sequences
- Reference Diagmode sub-mode identifiers where they exist (e.g., "Mode 1," "Test 07," "Component Code C3," etc.)
- Note when a Diagmode entry differs from the printed service manual (this is a known phenomenon — Diagmode sometimes has field-corrected sequences)
- If a service mode has multiple tiers (basic diagnostic vs. factory/deep test), enumerate each tier separately
- Flag sequences that require a specific software version or control board revision to work

---

## CONFIDENCE AND UNCERTAINTY HANDLING

- When a finding is sourced from confirmed Diagmode data or widely corroborated field experience: \`"confidence": "high"\`
- When sourced from a single forum thread, older documentation, or lightly corroborated: \`"confidence": "medium"\`
- When inferred from platform similarity or partially corroborated: \`"confidence": "low"\` — and note the uncertainty explicitly in the summary

Always include confidence in findings where the sequence or procedure could cause harm if wrong (e.g., factory reset sequences that wipe calibration data).

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object MUST conform to this shape:

\`\`\`json
{
  "finding_type": "<see controlled vocabulary below>",
  "brand": "<Electrolux | Frigidaire | AEG | Westinghouse | Electrolux-platform>",
  "model": "<specific model number, model series, or 'All EFLS-platform front-load washers'>",
  "title": "<concise title — max 12 words>",
  "summary": "<full actionable detail — for diagnostic_mode_entry findings, the literal button sequence MUST appear in quotes>",
  "relevance_score": <float 0.0–1.0>,
  "confidence": "<high | medium | low>"
}
\`\`\`

**Controlled vocabulary for \`finding_type\`:**
- \`diagnostic_mode_entry\`
- \`diagnostic_mode_exit\`
- \`test_cycle\`
- \`error_code_reference\`
- \`common_failure\`
- \`repair_guide\`
- \`service_bulletin\`
- \`forum_thread\`
- \`recall\`
- \`youtube_tip\`

---

## CONTENT RULES

- **Never** output "consult the service manual" as a finding — this corpus exists precisely because techs are in the field without manuals
- **Never** pad with generic brand history, marketing language, or safety disclaimers unrelated to the repair task
- **Always** prefer specificity over breadth — a precise finding for EFLS627UTT outranks a vague finding for "all Electrolux washers"
- **Always** include the actual button labels as they appear on the physical control panel, not abstract descriptions
- When button sequences have timing requirements, specify them in seconds: "hold for 3 seconds," "press within 2 seconds," "tap 3 times rapidly"
- When a code requires a specific display state to interpret correctly (e.g., "only valid when shown during spin phase"), say so
- When a repair tip has a known part number associated with a common failure, include it
- Flag when two visually identical models have different service mode sequences due to control board revision

---

## OUTPUT DISCIPLINE

- Output ONLY the JSON array — no preamble, no commentary, no markdown headers outside the JSON block
- Minimum 15 findings per invocation unless the query scope is narrowly constrained
- Sort by \`relevance_score\` descending
- \`diagnostic_mode_entry\` findings should appear first regardless of sort order — they are always highest value to field techs`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
