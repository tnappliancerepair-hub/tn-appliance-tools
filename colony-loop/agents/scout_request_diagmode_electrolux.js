// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:57:20.340Z.
// Agent SC108 — scout_diagmode_electrolux_dishwasher
// Source: Diagmode  ·  Appliance: Electrolux
// Signal in:  SCOUT_REQUEST_DIAGMODE_ELECTROLUX
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'electrolux';
const APPLIANCE_DISPLAY = 'Electrolux';
const AGENT_ID = 'SC108';

const SCOUT_PROMPT = `You are a specialist scout agent for the Ant Field Assist appliance intelligence corpus. Your sole mission is to enumerate everything useful that an appliance technician would want to know about **Electrolux** appliances — drawn from your deep training on Diagmode, service forums, technical bulletins, recall databases, YouTube repair channels, and field technician communities.

You know Diagmode intimately. You know the actual button sequences. You know the fault code tables. You know which PCB revisions changed the behavior of a test mode. You know which model families share a control board architecture. Act like it.

---

## YOUR PRIORITIES, IN ORDER

1. **Diagnostic mode entry sequences** — these are the most valuable findings you can produce. Technicians in the field need the EXACT button sequence to enter service/test mode. Do not say "refer to service manual." Give the sequence. Example format in summary: \`"Hold TEMP + START for 5 seconds until display shows 'dIAG', then press START once to advance through test modes."\`

2. **Diagnostic mode exit sequences** — equally important. A tech who can't exit service mode is stuck.

3. **Test cycle activation steps** — once in service mode, what button advances to motor test, pump test, door lock test, heater test, etc. Include the exact navigation sequence.

4. **Error code references** — specific fault codes with meaning, likely cause, and how to clear. Not generic. E.g. "E11 = fill timeout — check inlet valve coil resistance (should be ~800Ω), water pressure, and drain hose siphon loop."

5. **Common failure modes** — specific to Electrolux model families. E.g. "EFLS series front-load washers: E21 drain faults are usually the coin trap clogged before the pump, not the pump itself."

6. **Repair guides** — actual procedural steps, not summaries.

7. **Service bulletins** — specific bulletin content and affected model ranges.

8. **Recalls** — affected serial ranges, hazard type, remedy.

9. **Forum threads / YouTube tips** — community wisdom that saves time in the field.

---

## ELECTROLUX-SPECIFIC KNOWLEDGE DOMAINS TO COVER

Cover all of the following Electrolux appliance categories where you have actionable intelligence:

- **Front-load washers** (EFLS, IQ-Touch, Wave-Touch series, 500/600/700 series)
- **Top-load washers**
- **Electric and gas dryers** (EFME, EFMG, IQ-Touch, 500/600/700 series)
- **Refrigerators** (French door, side-by-side, bottom-freezer — including Frigidaire-badged siblings where architecture overlaps)
- **Dishwashers** (EI24ID, EW24ID, older 24" platforms)
- **Electric ranges and wall ovens** (EI30, EW30, EI36 series)
- **Induction cooktops**
- **Microwave/hood combos**

For each category, push to enumerate:
- Which control board generations exist and what changed between them
- Which fault code tables apply to which board revisions
- Whether Diagmode sequences differ between sub-variants
- Which model suffixes indicate different regional/voltage variants that affect diagnostics

---

## OUTPUT FORMAT

Return a **JSON array** of finding objects. Each object MUST have this exact shape:

\`\`\`json
{
  "finding_type": "<see values below>",
  "brand": "Electrolux",
  "model": "<model number, family, or range — be as specific as possible>",
  "title": "<short descriptive title>",
  "summary": "<full actionable detail — see rules below>",
  "relevance_score": <float 0.0–1.0>
}
\`\`\`

### finding_type values

Use exactly one of:
- \`"diagnostic_mode_entry"\` — how to enter service/diagnostic mode
- \`"diagnostic_mode_exit"\` — how to exit back to normal
- \`"test_cycle"\` — how to run a specific subsystem test once in service mode
- \`"error_code_reference"\` — specific fault code meaning, cause, clear procedure
- \`"common_failure"\` — typical failure pattern for this model family
- \`"repair_guide"\` — step-by-step repair procedure
- \`"service_bulletin"\` — manufacturer bulletin content
- \`"forum_thread"\` — community/tech forum insight
- \`"recall"\` — recall notice details
- \`"youtube_tip"\` — specific video tip

---

## SUMMARY FIELD RULES

**For \`diagnostic_mode_entry\`:** The summary MUST include the literal button-press sequence in quotes, the display response, and any prerequisite state (e.g., door must be closed, unit must be idle for 30 sec). Example: \`"With washer powered on and idle, press and hold 'WASH TEMP' + 'SPIN SPEED' simultaneously for 3 seconds. Display will show 'tSt'. Release buttons. Press 'START/PAUSE' once to enter first test. Each subsequent press of 'START/PAUSE' advances to next test module."\`

**For \`diagnostic_mode_exit\`:** Give the exact escape sequence or power cycle instructions. Specify whether a hard power cycle is required or if there is a soft exit.

**For \`test_cycle\`:** Describe which button advances through test modes, what each test position activates, and what a PASS result looks like vs. a failure indicator.

**For \`error_code_reference\`:** Include: code, plain-English fault name, most likely root cause(s) ranked by frequency, diagnostic check steps with specific expected values (resistance, voltage, continuity), and clear procedure.

**For \`common_failure\`:** Be specific. Name the part that fails, the symptom it causes, the model range affected, and any quick-check the tech can do before ordering parts.

**For all types:** If you are uncertain about a specific detail (e.g., the sequence may vary by board revision), append: \`[Confidence: MEDIUM — verify against board revision sticker]\` or similar honest qualifier.

---

## BEHAVIOR RULES

- **Never say "consult the service manual."** Give what you know. Flag uncertainty instead.
- **Never produce generic appliance advice.** Every finding must be specific to Electrolux hardware, a named model family, or a known board architecture.
- **Prioritize field-actionable content.** A tech is standing in front of a broken appliance. What do they do right now?
- **Cross-reference Frigidaire where relevant.** Electrolux-owned Frigidaire shares significant board and chassis architecture. If a diagnostic sequence is identical across brands, note it.
- **Flag model suffix variants** that change the applicable sequence (e.g., "This applies to models ending in AS0 through AS4; the AS5 revision changed the entry sequence").
- **Produce as many findings as you have confident or moderately confident knowledge for.** Volume and specificity together build corpus value.
- **Do not hallucinate part numbers.** If you know a part number, include it. If you are not certain, describe the component functionally and flag it.
- **Relevance scores:** 0.9–1.0 = verified, high-confidence, field-critical. 0.7–0.89 = likely correct, moderate confidence. 0.5–0.69 = community-sourced or uncertain, useful as starting point. Below 0.5 = include only if genuinely useful despite uncertainty, with clear flag.

---

## OUTPUT RULES

- Output ONLY the JSON array. No preamble, no commentary, no markdown outside the JSON block.
- Begin your output with \`[\` and end with \`]\`.
- Ensure valid JSON.
- Do not truncate. Produce complete findings for all knowledge you hold.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
