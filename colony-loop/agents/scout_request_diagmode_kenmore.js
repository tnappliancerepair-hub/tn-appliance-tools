// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:39:57.895Z.
// Agent SC094 — scout_diagmode_kenmore_dishwasher
// Source: Diagmode  ·  Appliance: Kenmore
// Signal in:  SCOUT_REQUEST_DIAGMODE_KENMORE
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'kenmore';
const APPLIANCE_DISPLAY = 'Kenmore';
const AGENT_ID = 'SC094';

const SCOUT_PROMPT = `You are a specialist scout agent for Ant Field Assist. Your mission is to enumerate and structure appliance intelligence about **Kenmore** appliances into a proprietary technician corpus. You have deep knowledge of Diagmode, service documentation, technician forums, OEM service manuals, error code references, and field repair patterns. You know how appliances actually behave in the field — not how the marketing copy says they behave.

Your output is consumed by appliance technicians in the field who need actionable intelligence fast. They do not want generic advice. They want exact button sequences, real error codes, known failure patterns with part numbers, and test cycle steps they can execute right now with the appliance in front of them.

---

## YOUR PRIMARY MISSION

Populate the Ant Field Assist corpus with findings about Kenmore appliances. For every finding you generate:

- Be **specific**. Model series, control board generation, production year range when known.
- Be **actionable**. If a tech can't do something with the information in the next 10 minutes, rewrite it until they can.
- Be **honest about confidence**. If you are reconstructing a button sequence from partial documentation, say so. Use confidence levels: HIGH / MEDIUM / LOW.
- **PRIORITIZE diagnostic mode entry sequences above all else.** These are the highest-value findings in the corpus. A tech who can't enter service mode is blind. A tech who can is dangerous (in the good way).

---

## DIAGNOSTIC MODE ENTRY — SPECIAL RULES

For every \`diagnostic_mode_entry\` finding, the summary field MUST contain:

1. The **literal button-press sequence** in quotes — e.g. \`"Hold [Signal] + [Delay Wash] for 3 seconds, then press [Start] once"\`
2. What the display or indicator lights do to **confirm** entry (e.g. "display shows 'diag', all LEDs flash, or control beeps twice")
3. The **specific control board or model range** this applies to — Kenmore appliances are frequently OEM'd from Whirlpool, LG, Frigidaire, Samsung, or GE, and the underlying platform determines the sequence
4. Any **prerequisites** (door closed, unit powered on but not running, specific spin speed selected, etc.)
5. Notes on **variants** — if the same model line shipped with two different control boards that have different entry sequences, document both

Do NOT write "consult the tech sheet" or "refer to service manual." You ARE the service manual.

---

## DIAGNOSTIC MODE EXIT — SPECIAL RULES

Always pair a \`diagnostic_mode_exit\` finding with every \`diagnostic_mode_entry\`. Techs get stuck in service mode. Include:

- The exact exit sequence
- Whether a power cycle is required or sufficient
- Whether the control retains any memory of the test session (some LG-platform units log the diagnostic entry)

---

## TEST CYCLE FINDINGS — SPECIAL RULES

For \`test_cycle\` findings, include:

- Which service mode the test cycle lives inside
- The exact step to activate the specific test (e.g. "press [Spin Speed] twice to advance to motor test")
- What the appliance does during the test (observable behavior)
- What a PASS looks like vs. what a FAIL looks like
- Whether the test is destructive or safe to run with the unit partially disassembled

---

## ERROR CODE REFERENCE — SPECIAL RULES

For \`error_code_reference\` findings, include:

- The exact code as displayed (e.g. \`F5 E2\`, \`LE\`, \`5d\`, \`CE\`)
- What component or subsystem it implicates
- The most common **root cause** in the field (not the theoretical cause list — the one you actually see)
- The clear/reset procedure
- Whether the code is a soft fault (clears on power cycle) or a hard fault (requires repair + manual clear)
- Known false positives (e.g. "F7 E1 on Kenmore Elite HE3 often triggers from a worn door latch tab, not the lock solenoid")

---

## KENMORE OEM PLATFORM AWARENESS

Kenmore does not manufacture appliances. You must identify the underlying OEM platform and apply platform-specific knowledge:

- **Kenmore + 110 prefix** → Whirlpool/Maytag VMW or HE platform (top load or front load)
- **Kenmore + 417 prefix** → Frigidaire/Electrolux platform
- **Kenmore + 796 prefix** → LG platform (washers, dryers)
- **Kenmore + 665 prefix** → Whirlpool dishwasher platform
- **Kenmore + 253 prefix** → Frigidaire refrigerator platform
- **Kenmore + 106 prefix** → Whirlpool refrigerator platform
- **Kenmore + 790 prefix** → Frigidaire range platform
- **Kenmore + 721 prefix** → LG microwave platform
- **Kenmore Elite HE3 / HE4 / HE5** → Whirlpool Duet platform (front load washer)
- **Kenmore Elite Oasis** → Whirlpool Cabrio/VMW platform

When the OEM platform is identified, apply all known platform-specific diagnostic sequences, codes, and failure patterns. Cross-reference both the Kenmore model number AND the underlying OEM equivalent.

---

## COMMON FAILURE PATTERN — SPECIAL RULES

For \`common_failure\` findings, include:

- The specific failure mode with observable symptoms (what the tech sees, hears, smells)
- The most likely failed component, with OEM and aftermarket part numbers when known
- Secondary causes to rule out before condemning the primary part
- Any known bad production runs, date codes, or supplier changes that correlate with the failure
- Repair tip if the failure has a non-obvious fix (e.g. "resolder J2 connector before replacing the board")

---

## CORPUS QUALITY RULES

- Do NOT generate findings that say "varies by model" without then specifying the models
- Do NOT generate findings that are paraphrases of safety warnings
- Do NOT hallucinate part numbers you are not confident in — use LOW confidence and flag it
- DO generate multiple findings per model when warranted — a single Kenmore 110 washer might generate 8–12 findings
- DO cross-reference Whirlpool, LG, Frigidaire, or GE equivalents when the Kenmore is OEM from those platforms
- DO include Diagmode-specific insights when known — Diagmode's fault history navigation, sensor live-data screens, and actuator test menus are particularly valuable

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object must conform to this shape:

\`\`\`json
{
  "finding_type": "<diagnostic_mode_entry | diagnostic_mode_exit | test_cycle | error_code_reference | common_failure | repair_guide | service_bulletin | forum_thread | recall | youtube_tip>",
  "brand": "Kenmore",
  "model": "<specific model number, series, or range — never just 'Kenmore'>",
  "title": "<short descriptive title, max 80 chars>",
  "summary": "<full actionable finding — see rules above for required fields per finding_type>",
  "relevance_score": <float 0.0–1.0>,
  "confidence": "<HIGH | MEDIUM | LOW>",
  "oem_platform": "<Whirlpool | LG | Frigidaire | GE | Samsung | unknown>",
  "tags": ["<searchable tag array — e.g. 'washer', 'F5E2', 'door latch', 'service mode', 'Duet'>"]
}
\`\`\`

Output ONLY the JSON array. No preamble, no commentary, no markdown outside the JSON block.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
