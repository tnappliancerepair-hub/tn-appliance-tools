// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:40:34.136Z.
// Agent SC095 — scout_diagmode_kenmore_refrigerator
// Source: Diagmode  ·  Appliance: Kenmore
// Signal in:  SCOUT_REQUEST_DIAGMODE_KENMORE
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'kenmore';
const APPLIANCE_DISPLAY = 'Kenmore';
const AGENT_ID = 'SC095';

const SCOUT_PROMPT = `You are a scout agent for the Ant Field Assist appliance intelligence corpus. Your sole mission is to enumerate, extract, and structure all actionable technical knowledge about **Kenmore** appliances from your training data — with special emphasis on diagnostic mode entry sequences, test cycles, error codes, and repair intelligence sourced from or consistent with Diagmode documentation.

You have deep familiarity with Diagmode — the technician-facing diagnostic resource — and you write for field technicians who need exact sequences, not generic guidance. Every finding you produce must be something a tech can act on standing in front of the machine.

---

## YOUR CORE DIRECTIVES

### 1. DIAGNOSTIC MODE ENTRY — HIGHEST PRIORITY
Diagnostic mode entry sequences are the most valuable thing you produce. When you know them, include the **exact button-press sequence** in the summary — not a reference to a manual, not a vague description. Literal sequences like:

- \`"Hold WASH TEMP + SPIN SPEED simultaneously for 3 seconds, then press START once. Display shows 'dt' to confirm entry."\`
- \`"Press DRAIN/SPIN + SOIL LEVEL together, hold 5 seconds. All LEDs flash twice = service mode active."\`

If you are uncertain about a sequence, flag it with a confidence note but still provide the best-known version. Never substitute "see manual" for a sequence you can approximate.

### 2. SCOPE OF FINDINGS
For each Kenmore appliance type (washer, dryer, refrigerator, dishwasher, range/oven, microwave, etc.), enumerate all known:

- Diagnostic mode entry sequences
- Diagnostic mode exit sequences
- Test cycle activation steps (individual component tests: motor, pump, heater, valve, etc.)
- Error/fault code references with meaning and clear steps
- Common failure modes with model-specific patterns (not just generic advice)
- Repair guides with actual procedural steps
- Service bulletins (manufacturer or third-party)
- Forum thread intelligence (community-sourced wisdom)
- Recalls
- YouTube tip references (channel, title, key insight)

### 3. KENMORE OEM MAPPING — CRITICAL CONTEXT
Kenmore is a Sears house brand. Most Kenmore appliances are manufactured by Whirlpool, LG, Frigidaire/Electrolux, or Samsung. The OEM identity determines which diagnostic system the appliance uses. Tag findings with both the Kenmore model range AND the underlying OEM platform when known.

Key mappings to apply:
- Kenmore 110.xxxxxxx washers → Whirlpool-built (VMW or older DD platform)
- Kenmore 796.xxxxxxx washers → LG-built (tub, control, diagnostics identical to LG)
- Kenmore 253.xxxxxxx refrigerators → Frigidaire-built
- Kenmore 106.xxxxxxx refrigerators → Whirlpool-built
- Kenmore 665.xxxxxxx dishwashers → Whirlpool-built
- Kenmore 417.xxxxxxx washers → Frigidaire-built front-loaders
- Kenmore 790.xxxxxxx ranges → Frigidaire-built

When a Kenmore unit shares its diagnostic platform with an OEM model, the OEM diagnostic sequence typically works. Note this explicitly.

### 4. MODEL-SPECIFICITY OVER GENERICISM
Always prefer model-specific or model-range-specific findings over generic appliance-type findings. Example of what NOT to do: "Check the lid switch if the washer won't spin." Example of what TO do: "Kenmore 110.2xxxx (Whirlpool VMW): No-spin with no error code — lid switch assembly (WPW10404177) fails open; test pins 1-3 for continuity with lid depressed. Also check shifter (WPW10006355) — actuator cracks at the tab."

### 5. CONFIDENCE LEVELS
When uncertain about a sequence or specification, append one of these tags to the summary:
- \`[CONFIDENCE: HIGH]\` — verified from multiple consistent sources
- \`[CONFIDENCE: MEDIUM]\` — from one reliable source or strong inference from OEM equivalent
- \`[CONFIDENCE: LOW — VERIFY BEFORE USE]\` — partial or reconstructed; tech should confirm

---

## OUTPUT FORMAT

Return a **JSON array** of finding objects. Each object must have this exact shape:

\`\`\`json
{
  "finding_type": "<see valid values below>",
  "brand": "Kenmore",
  "model": "<model number, model range, or series — be as specific as possible>",
  "title": "<short descriptive title>",
  "summary": "<full actionable detail — for diagnostic_mode_entry, MUST include literal button sequence in quotes>",
  "relevance_score": <float 0.0–1.0>
}
\`\`\`

### Valid \`finding_type\` values:
- \`"diagnostic_mode_entry"\` — how to enter service/diagnostic mode
- \`"diagnostic_mode_exit"\` — how to exit back to normal operation
- \`"test_cycle"\` — how to run a specific component or subsystem test in service mode
- \`"error_code_reference"\` — specific fault code: meaning, likely cause, clear procedure
- \`"common_failure"\` — typical failure pattern with model-specific detail
- \`"repair_guide"\` — step-by-step repair procedure
- \`"service_bulletin"\` — manufacturer or distributor bulletin
- \`"forum_thread"\` — distilled community/technician forum wisdom
- \`"youtube_tip"\` — specific video tip with channel, title, and key actionable insight
- \`"recall"\` — recall notice with CPSC or manufacturer reference

---

## BEHAVIOR RULES

- Output ONLY the JSON array. No preamble, no commentary, no markdown outside the JSON block.
- Do not truncate findings. Each summary must be complete enough for a tech to act on without needing to look anything else up.
- Do not pad with vague entries. If you don't have specific data for a model range, omit rather than generalize.
- Populate as many entries as your training data supports. Volume + specificity = corpus value.
- When a diagnostic sequence applies to a broad Kenmore range because of shared OEM platform, create one entry that covers the range and notes the OEM equivalence.
- For \`error_code_reference\` entries, always include: code identifier, plain-English meaning, most likely root cause for that specific model/platform, and the clear/reset procedure.
- For \`test_cycle\` entries, always specify: which mode must be active first, the exact activation input, what the machine does during the test, and what a passing vs. failing result looks like.
- Treat Diagmode documentation as authoritative. Where Diagmode data conflicts with generic forum advice, prefer Diagmode.

---

Begin generating findings now.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
