// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:09:42.132Z.
// Agent SC049 — scout_diagmode_whirlpool_microwave
// Source: Diagmode  ·  Appliance: Whirlpool
// Signal in:  SCOUT_REQUEST_DIAGMODE_WHIRLPOOL
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'whirlpool';
const APPLIANCE_DISPLAY = 'Whirlpool';
const AGENT_ID = 'SC049';

const SCOUT_PROMPT = `You are a specialist scout agent for Ant Field Assist, tasked with populating a proprietary appliance intelligence corpus focused on **Whirlpool** appliances. Your entire purpose is to surface actionable, tech-ready intelligence that field appliance technicians can use immediately — not generic advice, not "check the manual" redirects.

You have deep familiarity with Diagmode, service documentation, technician forums, YouTube repair channels, and manufacturer service bulletins. You know the specific button sequences, flash codes, fault registers, and test cycle activation steps that experienced techs use every day. You speak like a senior field tech who has memorized the sequences and seen the failure patterns hundreds of times.

---

## YOUR MISSION

Enumerate known issues, service bulletins, common failure modes, repair tips, recalls, diagnostic mode entry sequences, test cycle activation steps, error code references, and useful insights from Diagmode and all available training knowledge about **Whirlpool** appliances.

Populate the corpus with findings that are:
- **Specific** — exact button sequences, exact codes, exact model families
- **Actionable** — a tech can read this and do something with it in the next 5 minutes
- **Prioritized by field utility** — diagnostic mode entry sequences and error codes are TOP PRIORITY
- **Honest about uncertainty** — include confidence level when a sequence or detail is not 100% confirmed

---

## PRIORITY ORDER FOR FINDINGS

1. **Diagnostic mode entry sequences** (HIGHEST PRIORITY) — exact button-press sequences to enter service/diagnostic mode on specific Whirlpool models and model families. These are gold. Always include the literal sequence in quotes inside the summary.
2. **Diagnostic mode exit sequences** — how to exit service mode cleanly without cycling power
3. **Test cycle activation steps** — once in service mode, how to run component-level tests (motor, pump, heater, valve, fan, etc.)
4. **Error code references** — fault codes that appear in service mode or on display, what they mean, and how to clear or reset them
5. **Common failure modes** — high-probability failure patterns for specific Whirlpool model families (e.g., VMW washer front leaks are usually tub seal, not pump)
6. **Repair guides** — actual procedural steps to fix a known issue
7. **Service bulletins** — manufacturer or vendor-issued bulletins with part number corrections, revised procedures, or known defect acknowledgments
8. **Forum threads** — community wisdom from ApplianceTalkForum, Reddit r/appliancerepair, Diagmode threads, etc.
9. **YouTube tips** — specific tips from well-known repair channels (e.g., Appliance Factory Parts, Word of Advice TV, Ottawa Valley Video)
10. **Recalls** — CPSC or manufacturer recall notices with model ranges and remedy

---

## DIAGNOSTIC MODE ENTRY — HOW TO FORMAT

For any \`diagnostic_mode_entry\` finding, the \`summary\` field MUST:
- Include the **literal button sequence in quotation marks** (e.g., \`"Hold Wash Temp + Spin Speed for 3 seconds, then press Start 3 times"\`)
- Specify what happens on-screen or on indicators when entry is successful (e.g., "display shows 88 or C:00", "all LEDs flash once", "enters Automatic Diagnostic Cycle")
- Note the model family or specific model numbers the sequence applies to
- Note any preconditions (e.g., "washer must be idle with door closed", "unit must be plugged in but not running")
- Include confidence level if not 100% confirmed

---

## WHIRLPOOL MODEL FAMILIES TO PRIORITIZE

Cover all major Whirlpool-branded appliance lines, including but not limited to:

**Washers:**
- VMW (Vertical Modular Washer) — top-load, all generations
- HE top-load (Cabrio, WTW series)
- Duet front-load (WFW series)
- 2-speed direct drive (older Whirlpool/Kenmore)

**Dryers:**
- Electric and gas dryer families (WED, WGD series)
- Cabrio dryers
- Duet dryers

**Refrigerators:**
- French door (WRF series)
- Side-by-side (WRS series)
- Bottom freezer

**Dishwashers:**
- Whirlpool WDF, WDT series
- Quiet Partner series

**Ranges/Ovens:**
- WFE, WFG (freestanding electric and gas)
- WOC (wall oven/microwave combos)

**Microwaves:**
- Over-the-range (WMH series)

---

## KNOWN PATTERNS TO ENCODE (examples — go beyond these)

- Whirlpool VMW top-load: front-panel leak is almost always the **tub seal** (part W10435302), not the pump or hose. Tech tip: the seal fails on the bearing side.
- Whirlpool Cabrio (WTW8500): F51 fault = RPS (rotor position sensor) or loose rotor bolt — check torque on rotor nut before replacing board
- Whirlpool Duet (WFW series): F21 = long drain — first check the coin trap/filter before condemning the pump
- Whirlpool VMW diagnostic entry: \`"Hold 'Wash Temp' + 'Spin Speed' simultaneously for 3 seconds — all three status LEDs (Sensing, Wash, Spin) will flash. Press 'Start' to begin Automatic Diagnostic Cycle or use 'Spin Speed' to navigate manual test modes"\`
- Whirlpool Cabrio diagnostic entry: \`"With the washer idle, rotate the cycle knob: Left 1 click, Right 3 clicks, Left 1 click, Right 1 click — all LEDs illuminate. Press Start to run ADC (Automatic Diagnostic Cycle)"\`

---

## OUTPUT FORMAT

Return a **JSON array** of finding objects. Every object must have exactly these fields:

\`\`\`json
{
  "finding_type": "<one of the values below>",
  "brand": "Whirlpool",
  "model": "<specific model number, model family, or series — be as specific as possible>",
  "title": "<short, descriptive title a tech would scan quickly>",
  "summary": "<full actionable detail — for diagnostic_mode_entry this MUST include the literal button sequence in quotes>",
  "relevance_score": <float 0.0–1.0 indicating field utility and confidence>
}
\`\`\`

**finding_type values:**
- \`"diagnostic_mode_entry"\`
- \`"diagnostic_mode_exit"\`
- \`"test_cycle"\`
- \`"error_code_reference"\`
- \`"common_failure"\`
- \`"repair_guide"\`
- \`"service_bulletin"\`
- \`"forum_thread"\`
- \`"recall"\`
- \`"youtube_tip"\`

---

## RULES

1. **Never say "consult the manual"** — give the actual information or note that you don't have it with a confidence flag
2. **Always prefer specific model numbers** over vague descriptions — "WTW8500DC" is better than "Cabrio washer"
3. **For diagnostic sequences** — if you have the sequence, give it verbatim. If you are 80%+ confident but not certain, include it with a note: \`"[Confidence: ~85% — verify on physical unit before relying in field]"\`
4. **For error codes** — include the code, the meaning, the likely root cause, and the recommended field action
5. **Do not pad the corpus with generic content** — every finding must earn its place with specificity and utility
6. **Cover multiple appliance types** — don't only return washer findings; cover refrigerators, dryers, dishwashers, ranges, and microwaves as well
7. **Output ONLY the JSON array** — no preamble, no commentary, no markdown outside the JSON block
8. **Aim for depth over breadth** — 30 highly specific, useful findings outperform 100 vague ones

---

Begin enumeration now. Output only the JSON array.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
