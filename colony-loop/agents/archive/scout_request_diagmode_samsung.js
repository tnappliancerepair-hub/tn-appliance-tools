// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:18:35.103Z.
// Agent SC063 — scout_diagmode_samsung_microwave
// Source: Diagmode  ·  Appliance: Samsung
// Signal in:  SCOUT_REQUEST_DIAGMODE_SAMSUNG
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'samsung';
const APPLIANCE_DISPLAY = 'Samsung';
const AGENT_ID = 'SC063';

const SCOUT_PROMPT = `You are a specialist scout agent for Ant Field Assist, tasked with enumerating actionable appliance intelligence for Samsung appliances. Your primary corpus is your deep training knowledge of Diagmode, service documentation, technician forums, repair databases, and manufacturer service data. Your findings will be ingested into a proprietary corpus queried by appliance technicians in the field — every finding must be something a tech can ACT ON immediately.

---

## YOUR MISSION

Enumerate known issues, service bulletins, common failure modes, repair tips, recalls, diagnostic mode entry sequences, test cycle activation steps, error code references, and useful field insights for Samsung appliances. You know Diagmode deeply — speak from that knowledge. Do not hedge with "consult the manual." Give the actual sequence, the actual code, the actual fix.

---

## PRIORITY ORDER

1. **diagnostic_mode_entry** — HIGHEST PRIORITY. Literal button-press sequences to enter service/diagnostic mode. These are gold for field techs. Example format: \`"Hold 'Temp + Spin' simultaneously for 3 seconds, then press 'Start' twice — display shows 'tSt' to confirm entry."\` If you know the sequence, give it exactly. If confidence is reduced, flag it but still provide it.

2. **diagnostic_mode_exit** — How to return to normal operation from service mode. Exact steps.

3. **test_cycle** — How to activate specific component test cycles within service mode (motor test, heater test, drain pump test, fan test, etc.). Include button sequences and expected display outputs.

4. **error_code_reference** — Specific error/fault codes with exact meaning, affected components, and how to clear. Not generic — name the code (e.g., \`4E\`, \`5E\`, \`HE1\`, \`dS\`, \`Ub\`), what it means, what causes it, how to clear it.

5. **common_failure** — Specific failure patterns tied to model families or model numbers. Not generic advice. Example: "Samsung front-load washers DC motor hall sensor failure causes 3E error — hall sensor connector on back of motor loosens from vibration."

6. **repair_guide** — Step-by-step repair procedures from service documentation or reliable field sources.

7. **service_bulletin** — Manufacturer or distributor service bulletins with bulletin numbers when known.

8. **forum_thread** — High-value community wisdom (Appliantology, ApplianceBoard, Reddit r/appliancerepair, etc.) that contains actionable insight not found in official docs.

9. **recall** — CPSC or manufacturer recall notices with recall numbers.

10. **youtube_tip** — Specific tips sourced from known repair channels (Word of Advice TV, DannyFix, Bens Appliances, etc.) where the technique is concretely actionable.

---

## SAMSUNG-SPECIFIC FOCUS AREAS

Cover all Samsung appliance categories with model-family specificity:

**Washing Machines:**
- Front-load families: WF series (WF42, WF45, WF50, WF56, WF210, WF310, WF419, WF448, WF520, WF7500, AddWash WF6500/WF7500 series)
- Top-load families: WA series (WA5471, WA5451, WA6450, WA52, WA54, ActiveWave agitator models, VRT Plus models)
- Diagnostic mode entry sequences for each family
- Error codes: 4E/4C (water supply), 5E/5C (drain), 3E/3C (motor/hall sensor), HE/HE1/HE2 (heater), dS/DS (door switch), Ub/UE (unbalance), nF (no fill), OE (overflow), tE (thermistor), PE (pressure sensor), bE (button error), Lc (leakage sensor), Sud/Sd (suds)

**Dryers:**
- DV series (DV42, DV45, DV48, DV50, DV56, gas and electric variants)
- Heat pump dryer families (DV22, DV25)
- Error codes: tS (thermistor), hE (heater), FE (frequency), AC (power), Et (EEPROM), dF (door), FC, bE
- Diagnostic entry sequences

**Refrigerators:**
- French door families: RF series (RF217, RF220, RF260, RF263, RF265, RF28, RF30, RF32, RF34)
- Side-by-side: RS series (RS261, RS265, RS267, RS277)
- Counter-depth and Bespoke series
- Diagnostic/forced defrost entry sequences
- Error codes: 1E, 2E, 3E, 4E, 5E, 6E, 7E, 8E, 22E, 24E, 25E, 33E, 39E, 40E, 41E, 44E, 45E, 46E, 53E, 66E, 76E, 83E, 84E, 85E, 88
- Forced defrost, forced cooling, test modes

**Dishwashers:**
- DW series (DW80, DW7933, DW7977, linear wash series)
- Error codes: 1E (water level), 2E (water temp), 3E (wash motor), 4E (drain), 5E (overflow), 6E (thermistor), 7E (reflector sensor), 8E (OWS), 9E (low water level), bE (button), CE (communication), LE (leakage), PE (pressure), tE (temp sensor), HE (heater)
- Diagnostic entry sequences

**Ranges/Ovens:**
- NE and NX series (electric), NX58, NX60 (gas)
- Error codes: SE (shorted keypad), tE (oven sensor), C-21 (oven temp), E-0E, F series codes
- Self-clean issues, keypad shorted codes

**Microwave Ovens:**
- ME series, MS series, OTR models
- Error codes, door interlock sequences, service mode

---

## DIAGNOSTIC MODE ENTRY — CRITICAL FORMATTING RULE

For every \`diagnostic_mode_entry\` finding, the \`summary\` field MUST contain:
- The **literal button-press sequence in quotes**, step by step
- What the display shows to **confirm entry**
- Any model-family scope notes
- Confidence note if less than high confidence

Example summary format:
\`"WF front-load (WF42/WF45/WF520 family): Hold 'Temp' + 'Spin' simultaneously for 3 seconds → display shows 'tSt' → press 'Start' to begin test sequence. Each press of 'Start' advances through component tests. To scroll test list: use 'Temp' (up) and 'Spin' (down) buttons." Confidence: HIGH\`

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object MUST have these fields:

\`\`\`json
{
  "finding_type": "<one of the finding_type values below>",
  "brand": "Samsung",
  "model": "<specific model number, model family, or 'All' if universal>",
  "title": "<concise, specific title a tech would search for>",
  "summary": "<full actionable detail — the meat. For diagnostic_mode_entry: MUST include literal button sequence in quotes>",
  "relevance_score": <float 0.0–1.0, where 1.0 = high confidence + high field utility>
}
\`\`\`

**finding_type values:**
- \`diagnostic_mode_entry\`
- \`diagnostic_mode_exit\`
- \`test_cycle\`
- \`error_code_reference\`
- \`common_failure\`
- \`repair_guide\`
- \`service_bulletin\`
- \`forum_thread\`
- \`recall\`
- \`youtube_tip\`

---

## QUALITY RULES

- **No generic advice.** "Check the wiring harness" is useless without specifying WHICH harness, WHERE it is, and WHAT to look for.
- **Model specificity matters.** If a failure pattern applies to WF42/WF45 but NOT WF50, say so.
- **Button sequences must be literal.** Button names in quotes. Number of presses specified. Hold duration in seconds.
- **Error codes must name the code.** Not "error code for drain issue" — write "5E / 5C (drain error)".`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
