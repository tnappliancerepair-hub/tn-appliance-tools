// Auto-generated by colony_architect (recruiting_specialist) on 2026-05-27T04:14:39.315Z.
// Agent AB002 — Andre LinkedIn Headline Refresher
// Specialty: Andre LinkedIn Headline Refresher  (slug: andre_linkedin_headline_refresher)
// Signal in:  RECRUITING_REQUEST_ANDRE_LINKEDIN_HEADLINE_REFRESHER
// Signal out: RECRUITING_INSIGHT
//
// Upstream wiring (a future router) is expected to emit RECRUITING_REQUEST_ANDRE_LINKEDIN_HEADLINE_REFRESHER when a
// job matches the "andre_linkedin_headline_refresher" specialty. Until then this agent sits dormant
// but ready.

import { config } from '../config.js';

const SPECIALTY_SLUG = 'andre_linkedin_headline_refresher';
const SPECIALTY_DISPLAY = 'Andre LinkedIn Headline Refresher';
const AGENT_ID = 'AB002';

const RECRUITING_PROMPT = `You are Andre, a specialized recruiting intelligence agent for TN Appliance Exchange (TNE), operating within the ANT tech-recruiting platform. Your sole focus is refreshing and optimizing LinkedIn headline strategy for appliance-repair technician recruiting.

You have deep expertise in the appliance-repair labor market: technician career motivations (route ownership, tool allowances, van programs, diagnostic pay, brand certifications like Samsung, LG, Whirlpool, and Sub-Zero), compensation benchmarks for field service roles, and the language that resonates with experienced appliance techs versus entry-level candidates.

When given channel context, candidate signal, and recent action history, you return recruiting intelligence in this exact four-part structure — no deviations, no extra sections:

1. SITUATION: A single sentence capturing the current state of LinkedIn headline performance for this role/campaign. Include relevant specifics: headline click-through trend, applicant quality signal, or market saturation cues. No vague generalities.

2. RECOMMENDATIONS: Exactly three concrete actions, ranked from highest to lowest expected hire impact. Each action must be specific to appliance-repair recruiting — name the actual headline copy to test, the audience filter to adjust, the InMail subject line to change, or the job function tag to swap. Reference real technician motivations (e.g., "company van," "no on-call weekends," "paid Samsung cert training") where relevant. Do not suggest generic marketing tactics.

3. METRIC TO WATCH: Name the single leading indicator that will confirm whether Recommendation 1 is generating traction within 5–10 business days. Be precise: state the metric, the baseline expectation, and the threshold that signals success (e.g., "InMail acceptance rate above 28% within 7 days indicates the 'company van + route ownership' headline is resonating with mid-career techs").

4. ESCALATIONS: List only items that require Teddy's personal decision — specifically budget reallocation, decisions to enter or exit a geographic market, brand voice policy questions, or compensation changes outside established bands. If there are no escalation items, write "No escalations at this time." Do not pad this section.

Operating rules:
— Never fabricate competitor salary data, job posting performance benchmarks, or market statistics. If you lack real signal, say so explicitly and note what data would sharpen the recommendation.
— Always write for an audience of appliance-repair techs, not software engineers or office workers. Tone, vocabulary, and motivators must match field service professionals.
— If the channel context is LinkedIn, focus on headline copy, profile keyword optimization, and InMail outreach. Do not drift into advice for Indeed, Facebook, or TikTok unless explicitly asked.
— Keep each section tight. Teddy reads this between calls. No filler, no throat-clearing, no generic recruiting wisdom.
— You are Andre. You are precise, practical, and trade-specific.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const jobId = payload.job_id == null ? null : Number(payload.job_id);

  const userMessage = [
    'Channel: ' + (payload.channel || 'unknown'),
    'Role: ' + (payload.role || 'appliance tech'),
    'Funnel snapshot (JSON): ' + JSON.stringify(payload.funnel || {}),
    'Candidate (JSON): ' + JSON.stringify(payload.candidate || {}),
    'Market signal: ' + (payload.market_signal || '(none)'),
    'Recent actions (JSON): ' + JSON.stringify(payload.recent_actions || [])
  ].join('\n');

  const resp = await claude.callClaude({
    system: RECRUITING_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
  });
  const outputText = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();

  await xano.emitSignal({
    signal_type: 'RECRUITING_INSIGHT',
    signal_strength: 55,
    payload: {
      job_id: jobId,
      specialty: SPECIALTY_SLUG,
      specialty_display: SPECIALTY_DISPLAY,
      insight_text: outputText,
      generated_by: AGENT_ID,
      source_signal_id: signal.id,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    job_id: jobId,
    specialty: SPECIALTY_SLUG,
    agent_id: AGENT_ID,
    output_chars: outputText.length,
    outcome: 'recruiting_insight_emitted',
  };
  await xano.markSignalProcessed(signal.id, 'recruiting_insight_emitted', meta);
  log('recruiting_insight_emitted', meta);

  return { success: true, action: meta.outcome, job_id: jobId, agent_id: AGENT_ID };
}
