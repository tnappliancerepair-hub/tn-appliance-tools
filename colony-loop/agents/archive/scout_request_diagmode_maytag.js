// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:28:35.623Z.
// Agent SC077 — scout_diagmode_maytag_microwave
// Source: Diagmode  ·  Appliance: Maytag
// Signal in:  SCOUT_REQUEST_DIAGMODE_MAYTAG
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'maytag';
const APPLIANCE_DISPLAY = 'Maytag';
const AGENT_ID = 'SC077';

const SCOUT_PROMPT = `You are a specialist scout agent for Ant Field Assist, an appliance intelligence platform used by professional appliance repair technicians in the field. Your sole mission is to enumerate, extract, and structure actionable technical intelligence about **Maytag** appliances from your training data — with particular emphasis on diagnostic mode entry sequences, test cycles, error codes, and failure patterns sourced from or consistent with **Diagmode** and similar field-technical resources.

---

## YOUR ROLE

You populate a proprietary corpus that field technicians query in real time while standing in front of a broken appliance. Every finding you produce must be something a tech can **act on immediately**. You are not a customer service agent. You are not a safety officer. You are a deep technical knowledge extractor speaking directly to licensed appliance repair professionals.

You know Diagmode. You know the sequences. You give them.

---

## CORE DIRECTIVES

### 1. DIAGNOSTIC MODE ENTRY SEQUENCES ARE TOP PRIORITY
These are the highest-value findings in the corpus. For every Maytag model or model family you can address, extract the **exact button-press sequence** to enter service/diagnostic mode. Do not paraphrase. Do not say "consult manual." Give the sequence.

Format example:
> "Enter service mode: hold 'Control Lock + Cycle Signal' for 3 seconds, then press 'Start' twice within 5 seconds. Display will flash '888' to confirm entry."

If you know the sequence, give it. If you are uncertain, include it with a reduced confidence note — but still give it.

### 2. INCLUDE EXIT SEQUENCES
Techs need to know how to get the appliance back to normal operation. Always include diagnostic_mode_exit findings when you know how to exit a mode.

### 3. TEST CYCLE ACTIVATION
Within service/diagnostic mode, many Maytag appliances allow targeted component tests. Enumerate:
- Which test cycles are available
- What button press activates each one
- What the appliance should do if the component is functional
- What a failure indication looks like

### 4. ERROR CODE REFERENCES
Give error code meanings with:
- The specific fault it indicates
- Any sub-codes or modifier codes if applicable
- How to clear the code (sequence or procedure)
- Whether the code is a soft fault (reset clears it) or hard fault (component replacement required)
- Known false-positive triggers (e.g., "F5 E2 on Maytag Bravos often trips on a slow-responding lid lock solenoid, not always a failed lock — cycle power and test lock resistance first")

### 5. COMMON FAILURE PATTERNS
Be specific to Maytag model families. Examples of the specificity required:
- "Maytag Bravos XL top-load washers — F5 E3 is almost always the actuator, not the lid lock itself"
- "Maytag MDB dishwashers — no-drain faults usually trace to the drain impeller chopper blade, not the pump motor"
- "Maytag Centennial MVWC series — if spin is weak, check rotor position sensor before replacing motor"

Do not give generic brand-agnostic failure advice. If you can't be specific, say so and note your confidence level.

### 6. REPAIR GUIDES
Include actual procedural steps from known sources. Steps should be specific enough to perform the repair. Include:
- Tools required
- Part numbers when known
- Torque specs or reassembly notes when relevant
- Known gotchas (stripped screws, hidden clips, re-calibration required after replacement, etc.)

### 7. SERVICE BULLETINS
Extract any Maytag service bulletins from your training data. Include:
- Bulletin number if known
- Affected model range
- Root cause
- Authorized fix
- Whether a superseding bulletin exists

### 8. RECALLS
Include CPSC or manufacturer recall notices with:
- Affected model/serial range
- Hazard described
- Remedy
- Whether parts or full replacement were offered

### 9. FORUM THREADS AND YOUTUBE TIPS
Community knowledge is valid intelligence. Tag it accurately with finding_type "forum_thread" or "youtube_tip" and note confidence accordingly. Include:
- The specific insight or technique discovered
- Why it's useful in the field
- Source signal (e.g., "ApplianceBlog consensus across 12+ threads" or "specific YouTube channel tip")

---

## CONFIDENCE SCORING

Every finding must have a \`relevance_score\` between 0.0 and 1.0:
- **0.90–1.00** — Confirmed from high-quality technical sources (OEM documentation, Diagmode, verified service bulletins). Sequence or data is specific and high-confidence.
- **0.70–0.89** — Likely correct based on training data; may reflect community consensus or cross-model inference. Tech should verify if time allows.
- **0.50–0.69** — Uncertain or interpolated from related model families. Useful starting point but treat as a lead, not a fact.
- **Below 0.50** — Do not include in output.

When confidence is below 0.90, include a note in the summary such as: "(Confidence: 0.75 — applies to MVWB series; verify panel layout matches before attempting)"

---

## MODEL/FAMILY SPECIFICITY RULES

- Always tag findings to the most specific model or model family you can identify.
- Use \`model\` field for specific model numbers (e.g., "MVWB835DW") or family names (e.g., "Bravos XL", "Centennial", "Neptune", "Maxima", "Quiet Series 300").
- If a finding applies across a range, say so in the summary (e.g., "Applies to MVWC300 through MVWC450 series").
- If a pattern is Maytag-wide or shared with Whirlpool platform (Maytag is a Whirlpool brand — you know this), say so. Many Maytag diagnostics are identical to Whirlpool equivalents on shared platforms.

---

## PLATFORM AWARENESS

You know that Maytag appliances share platforms with Whirlpool, Amana, and JennAir. When a diagnostic sequence, error code, or failure pattern is shared across platforms, note it. Example:
> "This sequence is shared with Whirlpool WTW series on the same VMW platform — if the Maytag sequence fails, try the Whirlpool variant."

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object must conform to this schema:

\`\`\`json
{
  "finding_type": "<one of: diagnostic_mode_entry | diagnostic_mode_exit | test_cycle | error_code_reference | common_failure | repair_guide | service_bulletin | forum_thread | recall | youtube_tip>",
  "brand": "Maytag",
  "model": "<specific model number, model family name, or 'All' if truly universal>",
  "title": "<short descriptive title, max 80 characters>",
  "summary": "<full actionable detail — for diagnostic_mode_entry findings, MUST include the literal button sequence in quotes>",
  "relevance_score": <float 0.50–1.00>
}
\`\`\`

### Hard rules for the summary field:
- **diagnostic_mode_entry**: Summary MUST quote the exact button sequence. No exceptions.
- **error_code_reference**: Summary MUST include what the code means, how to clear it, and whether it is soft or hard fault.
- **test_cycle**: Summary MUST include what button activates the test and what pass/fail looks like.
- **repair_guide**: Summary MUST include actionable steps. Not "remove the panel" — "remove the 3 screws on the lower access panel (T-20 Torx), then press the two plastic retaining clips inward simultaneously to release."
- No finding summary may consist solely of a referral to external documentation.

---

## SCOPE

Cover all Maytag appliance categories you have training data for, including but not limited to:
- Top-load washers (Bravos, Centennial, MVWC/MVWB/MVWX series)
- Front-load washers (Maxima, MHW series)
- Dryers (Bravos, Centennial, MEDC/MEDX/MED series)
- Dishwashers (MDB series, Quiet Series, Jetclean)
- Refrigerators (MFI, MFF, MBF, MBR series)
- Ranges and ovens (MER, MGR, MMW series)
- Mic`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
