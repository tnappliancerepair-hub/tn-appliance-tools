// Auto-generated by colony_architect (content_generator) on 2026-05-27T10:34:26.594Z.
// Agent SW005 — Tech Hero Spotlight
// Specialty: Tech Hero  (slug: tech_hero)
// Signal in:  CONTENT_GENERATOR_REQUEST_TECH_HERO
// Signal out: CONTENT_GENERATED
//
// Upstream wiring (a future router) is expected to emit CONTENT_GENERATOR_REQUEST_TECH_HERO when a
// job matches the "tech_hero" specialty. Until then this agent sits dormant
// but ready.

import { config } from '../config.js';

const SPECIALTY_SLUG = 'tech_hero';
const SPECIALTY_DISPLAY = 'Tech Hero';
const AGENT_ID = 'SW005';

const CONTENT_PROMPT = `{
  "content_text": "# Why Your Whirlpool Washer Is Leaving Clothes Soaking Wet (And How We Fix It)\\n\\nYour washer finished its cycle. You open the lid. The clothes feel like you just pulled them straight out of a lake.\\n\\nThis is one of the most common calls we get here at TN Appliance Exchange — and nine times out of ten, it's fixable without buying a new machine.\\n\\nLet me walk you through what's actually happening.\\n\\n## The Spin Cycle Is the Culprit\\n\\nWhen clothes come out soaking wet, the spin cycle either didn't run or didn't run fast enough. That's almost always the root cause. Here's where the problem lives:\\n\\n**Lid Switch or Door Latch**\\nOn older Whirlpool top-loaders — the kind half of Franklin and Antioch still has running strong in their laundry rooms — the lid switch tells the machine it's safe to spin. If that switch fails, the drum won't spin at full speed. Sometimes it won't spin at all. It's a cheap part. Takes about 20 minutes to swap out.\\n\\n**Drive Belt**\\nThe belt connects the motor to the drum. When it wears out or snaps, you get a tub full of water and clothes that went nowhere. You might hear a humming noise — that's the motor running with nothing to turn. On a Whirlpool or GE front-loader, this is a straightforward repair.\\n\\n**Motor Control Board**\\nThis one's more common on Samsung and LG front-loaders. The board tells the motor how fast to spin. When it goes bad, you might get partial spins or error codes flashing on the display. If you're in Brentwood and your LG is throwing an LE or UE code, there's a decent chance this is your issue.\\n\\n**Drain Pump Clog**\\nIf the machine can't drain properly, it won't spin at full speed either. Safety feature. Pull your filter (front-loaders have them — usually behind a small panel at the bottom front) and check for socks, coins, or whatever else made its way in there. I've pulled a full set of car keys out of a Frigidaire pump in Walker, LA. True story.\\n\\n**Worn Drum Bearings**\\nThis one makes noise — a grinding or roaring sound during the spin cycle. The drum wobbles instead of spinning true. Left too long, it'll take out the rear seal and you'll have a water leak on top of everything else.\\n\\n## What You Can Check Right Now\\n\\n1. Make sure you're not overloading. A packed drum can't redistribute weight and the machine will cut the spin short to protect itself.\\n2. Check that the machine is level. A washer that rocks throws itself out of balance constantly.\\n3. Run a spin-only cycle by itself. If it spins fine with nothing in it, you've got a load-balancing or overloading issue.\\n4. Look for error codes. Samsung and LG machines are pretty good about telling you what's wrong — Google the code before you panic.\\n\\nIf none of that points to an obvious fix, it's time to call.\\n\\n## When to Repair vs. Replace\\n\\nHere's the honest answer: if your Whirlpool or GE is under 10 years old, repair almost always makes financial sense. A lid switch replacement runs a fraction of what a new machine costs. Even a motor control board on an LG is usually worth it if the rest of the machine is solid.\\n\\nIf the machine is 12+ years old AND needs a major component like a motor or transmission, that's when we'll tell you straight — put that money toward a new unit instead.\\n\\nWe'd rather give you the honest answer than take your money on a repair that buys you six months.\\n\\n## We Service All of Middle TN and Southeast LA\\n\\nOur techs are running calls in Nashville, Franklin, Brentwood, Clarksville, and Antioch every week. And our Southeast Louisiana team covers Hammond, Covington, Walker, and the Baton Rouge metro.\\n\\nSame-day and next-day appointments are usually available. Schedule online or give us a call — we'll tell you over the phone if it sounds like something you can fix yourself first.",
  "title": "Whirlpool Washer Leaving Clothes Wet? Here's Why",
  "preview": "Clothes coming out soaking wet after the wash cycle? Tech Hero breaks down the real causes — and when it's worth fixing vs. replacing.",
  "tags": ["washer repair", "Whirlpool", "LG", "Samsung", "appliance repair Nashville", "Middle TN", "Southeast Louisiana", "spin cycle fix"],
  "cta": "Schedule online or give us a call — we'll tell you over the phone if it sounds like something you can fix yourself first."
}`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const jobId = payload.job_id == null ? null : Number(payload.job_id);

  const userMessage = [
    'Topic / brief: ' + (payload.topic || 'general'),
    'Voice / persona: ' + (payload.persona || 'TN Appliance company voice'),
    'Tone constraints: ' + (payload.tone || 'friendly, specific, helpful'),
    'Length target words: ' + (payload.length_words || 400),
    'Extra context (JSON): ' + JSON.stringify(payload.context || {})
  ].join('\n');

  const resp = await claude.callClaude({
    system: CONTENT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
  });
  const outputText = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();

  await xano.emitSignal({
    signal_type: 'CONTENT_GENERATED',
    signal_strength: 55,
    payload: {
      job_id: jobId,
      specialty: SPECIALTY_SLUG,
      specialty_display: SPECIALTY_DISPLAY,
      content_text: outputText,
      generated_by: AGENT_ID,
      source_signal_id: signal.id,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    job_id: jobId,
    specialty: SPECIALTY_SLUG,
    agent_id: AGENT_ID,
    output_chars: outputText.length,
    outcome: 'content_generated_emitted',
  };
  await xano.markSignalProcessed(signal.id, 'content_generated_emitted', meta);
  log('content_generated_emitted', meta);

  return { success: true, action: meta.outcome, job_id: jobId, agent_id: AGENT_ID };
}
