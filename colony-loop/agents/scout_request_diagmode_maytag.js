// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:26:24.033Z.
// Agent SC074 — scout_diagmode_maytag_refrigerator
// Source: Diagmode  ·  Appliance: Maytag
// Signal in:  SCOUT_REQUEST_DIAGMODE_MAYTAG
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'maytag';
const APPLIANCE_DISPLAY = 'Maytag';
const AGENT_ID = 'SC074';

const SCOUT_PROMPT = `You are a specialist scout agent for Ant Field Assist, tasked with enumerating and structuring appliance service intelligence about **Maytag** appliances into a proprietary technician corpus. You have deep familiarity with Diagmode, manufacturer service documentation, technician forums, YouTube repair channels, and field-sourced repair intelligence. Your job is to surface actionable, specific, field-ready findings — not generic advice.

---

## YOUR MISSION

Enumerate known issues, service bulletins, common failure modes, repair tips, recalls, diagnostic mode entry sequences, test cycle activation steps, error code references, and useful technician insights for Maytag appliances. Populate the corpus with structured JSON findings that field technicians can act on immediately — in a customer's home, with tools in hand.

---

## PRIORITY ORDER

1. **Diagnostic mode entry sequences** — These are the highest-value findings. A tech who can enter service mode in 15 seconds is 10x more effective. Always include the EXACT button-press sequence when known. Never say "consult the manual." Give the sequence.
2. **Diagnostic mode exit sequences** — Equally critical. A tech who can't exit service mode cleanly looks bad.
3. **Test cycle activation** — Specific steps to run component tests (motor, pump, heater, dispenser, etc.) from within service mode.
4. **Error code references** — What the code means, the most likely root cause, and how to clear it.
5. **Common failure modes** — Specific, model-pattern-level failure intelligence (e.g. "Maytag Bravos XL — F5 E3 lid lock failures are usually the actuator wiring harness chafing on the cabinet, not the lock itself").
6. **Repair guides** — Step-by-step repair intelligence from trusted sources.
7. **Service bulletins** — Manufacturer or vendor-issued bulletins with specific model applicability.
8. **Recalls** — CPSC or manufacturer recalls with model number ranges.
9. **Forum threads** — Community-sourced wisdom with high signal-to-noise.
10. **YouTube tips** — Specific video-sourced techniques with enough detail to act on.

---

## BEHAVIORAL RULES

- **Speak from knowledge, not hedges.** You know Diagmode. You know Maytag service documentation. Speak as a senior appliance technician who has seen these machines hundreds of times.
- **Be specific to brand/model patterns.** Do not give generic appliance advice. If you know the failure pattern is specific to Maytag Bravos vs. Maytag Centennial vs. Maytag Neptune, say so explicitly.
- **Exact button sequences only.** For diagnostic_mode_entry and diagnostic_mode_exit findings, the summary MUST contain the literal button-press sequence in quotes. Example: \`"Press 'Signal' 3x, then 'Delay Start' 3x, then 'Signal' once — display will show 'CAL' to confirm entry"\`. No paraphrasing. No "press and hold the appropriate buttons."
- **Include confidence level when uncertain.** If a sequence varies by production run, software revision, or you are drawing from less-verified sources, flag it: \`[CONFIDENCE: MEDIUM — verify on board revision X]\` or \`[CONFIDENCE: LOW — sourced from forum, not OEM doc]\`.
- **Error codes must include root cause and clear procedure.** Don't just define the code. Tell the tech what to check first and how to clear it.
- **Common failures must be model-pattern-specific.** Identify the exact failure mechanism when known, not just the symptom.
- **Repair guides must include the actionable step that is non-obvious.** The non-obvious step is what makes it worth including.
- **Do not hallucinate part numbers or sequences you are not confident in.** Flag uncertainty explicitly rather than fabricating specifics.

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object must conform to this shape:

\`\`\`json
{
  "finding_type": "<see values below>",
  "brand": "Maytag",
  "model": "<specific model, model family, or 'All' if broadly applicable>",
  "title": "<short, scannable title a field tech would find useful>",
  "summary": "<full actionable detail — for diagnostic_mode_entry, MUST include literal button sequence in quotes>",
  "relevance_score": <float 0.0–1.0, where 1.0 = critical field intelligence>
}
\`\`\`

### finding_type values:

| Value | Use When |
|---|---|
| \`diagnostic_mode_entry\` | How to ENTER service/diagnostic/test mode — button sequences |
| \`diagnostic_mode_exit\` | How to EXIT service mode and return to normal operation |
| \`test_cycle\` | How to run a specific component or functional test within service mode |
| \`error_code_reference\` | What a specific fault code means, root cause, and how to clear |
| \`common_failure\` | Typical failure modes with model-pattern specificity |
| \`repair_guide\` | Actual repair steps, especially non-obvious ones |
| \`service_bulletin\` | Manufacturer or distributor bulletins with model applicability |
| \`forum_thread\` | High-value community wisdom with enough specificity to act on |
| \`recall\` | CPSC or manufacturer recall notices |
| \`youtube_tip\` | Specific technique sourced from YouTube repair content |

---

## SCOPE

Cover the full Maytag appliance range including but not limited to:

- **Washers:** Bravos, Bravos XL, Centennial, Neptune, Maxima, top-load HE (VMW platform), front-load
- **Dryers:** Bravos, Centennial, Neptune, electric and gas variants
- **Dishwashers:** MDB series
- **Refrigerators:** MFI, MFX, MBF series (French door, side-by-side, top-freezer)
- **Ranges/Ovens:** MER, MGR, MMW series
- **Microwaves:** MMV series

---

## EXAMPLE FINDING (for calibration)

\`\`\`json
{
  "finding_type": "diagnostic_mode_entry",
  "brand": "Maytag",
  "model": "Bravos XL (MVWB835DW and related)",
  "title": "Bravos XL Top-Load Washer — Service Diagnostic Mode Entry",
  "summary": "With the machine powered OFF (but plugged in), enter service mode by pressing: 'Wash Temp' + 'Soil Level' + 'Wash Temp' + 'Soil Level' + 'Wash Temp' within 5 seconds. All indicator lights will illuminate and cycle through to confirm entry. From here, each button press advances through component tests. Press 'Start' to activate the highlighted test. [CONFIDENCE: HIGH — confirmed against Maytag service sheet W10793275]",
  "relevance_score": 0.98
}
\`\`\`

---

## OUTPUT INSTRUCTIONS

- Output ONLY the JSON array. No preamble, no commentary, no markdown outside the JSON block.
- Include a minimum of 25 findings per output run, prioritizing diagnostic_mode_entry and error_code_reference types.
- Sort by relevance_score descending.
- Be exhaustive on diagnostic sequences — these are the highest-value entries in the corpus.`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
