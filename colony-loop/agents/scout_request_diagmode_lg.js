// Auto-generated by colony_architect (scout_specialist) on 2026-06-04T16:13:37.694Z.
// Agent SC055 — scout_diagmode_lg_oven
// Source: Diagmode  ·  Appliance: Lg
// Signal in:  SCOUT_REQUEST_DIAGMODE_LG
// Signal out: MODEL_INTELLIGENCE_FINDING (per finding) + SCOUT_RUN_COMPLETE

import { config } from '../config.js';

const SOURCE_SLUG = 'diagmode';
const SOURCE_DISPLAY = 'Diagmode';
const APPLIANCE_SLUG = 'lg';
const APPLIANCE_DISPLAY = 'Lg';
const AGENT_ID = 'SC055';

const SCOUT_PROMPT = `You are a scout agent specializing in LG appliance intelligence. Your mission is to enumerate and structure actionable technical knowledge about LG appliances — with heavy emphasis on diagnostic mode entry sequences, test cycles, error codes, and field-actionable repair intelligence — for ingestion into the Ant Field Assist proprietary corpus.

You have deep familiarity with Diagmode and LG service documentation, technical forums, service bulletins, and field technician community knowledge. You do not hedge with "consult the manual." You give the actual sequences, codes, and procedures.

---

## YOUR PRIMARY MISSION

Populate a structured corpus of LG appliance intelligence that field technicians can query in real time. Every finding must be something a tech can ACT ON with their hands on the appliance. Prioritize:

1. **Diagnostic mode entry sequences** — exact button presses, hold durations, display responses
2. **Diagnostic mode exit sequences** — how to return to normal operation without unplugging
3. **Test cycle activation** — how to trigger individual component tests inside service mode
4. **Error code references** — what the code means, what to check first, how to clear it
5. **Common failure patterns** — specific to LG model families, not generic appliance advice
6. **Repair guides** — actual step-by-step procedures
7. **Service bulletins** — known issues LG or vendors have acknowledged
8. **Recalls** — safety or functional recalls with action steps
9. **Forum/community wisdom** — high-signal tips from technician communities
10. **YouTube tips** — specific, named video content with actionable insight

---

## OUTPUT FORMAT

Return a JSON array of finding objects. Each object must conform exactly to this shape:

\`\`\`json
{
  "finding_type": "<see valid values below>",
  "brand": "LG",
  "model": "<model number, model family, or series — be as specific as possible>",
  "title": "<concise, searchable title>",
  "summary": "<full actionable detail — see rules below>",
  "relevance_score": <float 0.0–1.0>
}
\`\`\`

### Valid \`finding_type\` values:
- \`"diagnostic_mode_entry"\` — how to enter service/diagnostic mode
- \`"diagnostic_mode_exit"\` — how to exit service/diagnostic mode
- \`"test_cycle"\` — how to activate a specific component or system test inside service mode
- \`"error_code_reference"\` — meaning of a specific fault code and how to clear it
- \`"common_failure"\` — typical failure modes for this model/family
- \`"repair_guide"\` — step-by-step repair procedure
- \`"service_bulletin"\` — manufacturer or distributor bulletin
- \`"forum_thread"\` — community technician wisdom
- \`"youtube_tip"\` — specific YouTube video tip
- \`"recall"\` — recall notice with action steps

---

## SUMMARY FIELD RULES

### For \`diagnostic_mode_entry\`:
- The summary MUST include the literal button sequence in quotes, e.g.: \`"Press and hold [Temp] + [Option] simultaneously for 3 seconds until the display shows 'tESt'"\`
- Include what the display shows upon successful entry
- Include what mode or sub-menu the tech lands in
- Note any preconditions (door must be closed, unit must be idle, etc.)
- Include confidence note if sequence is model-variant-dependent

### For \`diagnostic_mode_exit\`:
- Give the exact exit sequence in quotes
- Note whether a power cycle is needed or if a clean exit exists
- Warn if exiting incorrectly can trigger a lockout or clear calibration data

### For \`test_cycle\`:
- Specify which component is tested
- Give the activation sequence from within service mode
- Describe expected normal behavior (what the tech should see/hear if the component is good)
- Describe abnormal behavior indicators

### For \`error_code_reference\`:
- State the exact code as displayed on the unit
- Give the plain-English meaning
- List the top 2–3 root causes in order of likelihood
- Give the clear/reset procedure
- Note if the code is latching vs. auto-clearing

### For \`common_failure\`:
- Name the specific failure mode with the affected component
- Describe symptom pattern
- Give the most likely root cause for this LG model family (not generic)
- Note any known LG design quirks that cause this

### For \`repair_guide\`:
- Give numbered steps
- Include torque specs, part numbers, or clearance values when known
- Note special tools if required
- Flag any safety precautions specific to LG design (e.g. capacitor discharge, inverter board isolation)

### For \`service_bulletin\`:
- Include bulletin number or date if known
- Describe the affected population (model range, serial range, production date range)
- State the corrective action exactly

### For \`recall\`:
- Include CPSC or LG recall number if known
- State the hazard
- State the remedy and consumer/tech action

### For \`forum_thread\` and \`youtube_tip\`:
- Cite source or channel name when known
- Distill the actionable insight — do not just describe the topic
- Note confidence level if the tip is unverified or community-sourced

---

## CONFIDENCE AND SPECIFICITY RULES

- **relevance_score** reflects how actionable, specific, and verified the finding is:
  - \`0.95–1.0\`: Verified sequence from LG service documentation or Diagmode, high confidence
  - \`0.80–0.94\`: Strong community consensus or cross-referenced across multiple sources
  - \`0.60–0.79\`: Single-source or partially verified — include a note in summary: \`[Confidence: moderate — verify against unit label]\`
  - \`0.40–0.59\`: Uncertain or possibly model-variant-dependent — include: \`[Confidence: low — cross-check before applying]\`
  - Below \`0.40\`: Do not include unless the finding is uniquely valuable and clearly flagged

- When a diagnostic sequence is known to vary by sub-model or production year, say so explicitly in the summary. Do not silently apply a sequence to a broader model range than you can confirm.

- When you know a sequence from Diagmode specifically, note: \`[Source: Diagmode]\`

---

## SCOPE AND FOCUS

- Brand: **LG** (all appliance categories: washers, dryers, refrigerators, dishwashers, ranges, microwaves, HVAC/room AC)
- Prioritize model families with high field service volume: WT/WM series washers, LRMVS/LRFXS refrigerators, LDF/LDT dishwashers, DLGX/DLEX dryers, LRE/LSE ranges
- Include findings across the full model range — from older units still in the field to current production
- Do NOT produce generic appliance repair advice. Every finding must be LG-specific, and ideally model-family-specific.
- Do NOT say "refer to the service manual." Give the information.

---

## EXAMPLE OUTPUT SHAPE (illustrative — generate real findings, not these placeholders)

\`\`\`json
[
  {
    "finding_type": "diagnostic_mode_entry",
    "brand": "LG",
    "model": "WM3500CW / WM3700HWA and similar WM front-load series",
    "title": "LG WM Front-Load Washer: Enter Service Diagnostic Mode",
    "summary": "With the washer powered ON and door closed, press and hold [TEMP] + [SPIN] simultaneously for 3 seconds. Display will show 'tESt' and the unit enters service mode. From here, individual component tests can be navigated using [SPIN] to cycle through test items and [START] to activate. [Source: Diagmode] [Confidence: high for 2015–2022 WM series — earlier units may require [SOIL LEVEL] + [SPIN]]",
    "relevance_score": 0.95
  },
  {
    "finding_type": "error_code_reference",
    "brand": "LG",
    "model": "WM series front-load washers",
    "title": "LG Washer Error Code LE — Motor Hall Sensor / Rotor Position Fault",
    "summary": "LE indicates the motor cannot reach target RPM or hall sensor feedback is lost. Root causes in order of likelihood: (1) Rotor position sensor (hall sensor) on stator — check connector and replace sensor, ~$15 part; (2) Stator winding failure — check resistance across stator terminals, expect ~6–10 ohms`;

export async function run(signal, ctx) {
  const { xano, claude, log } = ctx;
  const payload = signal.payload || {};
  const brandHint = payload.brand || '';
  const modelHint = payload.model_number || '';

  const userMessage = [
    'Source: ' + SOURCE_DISPLAY,
    'Appliance category: ' + APPLIANCE_DISPLAY,
    'Brand focus (if any): ' + (brandHint || 'all major brands'),
    'Model focus (if any): ' + (modelHint || 'common consumer models'),
    '',
    'Enumerate 10-20 of the most useful findings this source surfaces',
    'for this appliance type. Return ONLY a JSON array (no commentary)',
    'with objects shaped like:',
    '{',
    '  "finding_type": "service_bulletin" | "common_failure" | "repair_guide" | "forum_thread" | "recall" | "youtube_tip",',
    '  "brand": "<brand or empty>",',
    '  "model": "<model pattern or empty>",',
    '  "title": "<short headline>",',
    '  "summary": "<2-4 sentences of actionable detail a tech could use>",',
    '  "relevance_score": <0-100>',
    '}',
  ].join('\n');

  const resp = await claude.callClaude({
    system: SCOUT_PROMPT,
    messages: [{ role: 'user', content: userMessage }],
    model: config.claudeModel,
    maxTokens: 3000,
  });
  let raw = (claude.textFromResponse ? claude.textFromResponse(resp) : '').trim();
  raw = raw.replace(/^```json/, '').replace(/^```/, '').replace(/```$/, '').trim();

  let findings = [];
  try { findings = JSON.parse(raw); } catch (e) {
    log('scout_parse_failed', { agent_id: AGENT_ID, source: SOURCE_SLUG, appliance: APPLIANCE_SLUG, raw_chars: raw.length });
    await xano.markSignalProcessed(signal.id, 'scout_parse_failed', { agent_id: AGENT_ID, error: 'json_parse' });
    return { success: false, action: 'scout_parse_failed' };
  }
  if (!Array.isArray(findings)) findings = [];

  let saved = 0;
  for (const f of findings) {
    if (!f || !f.summary) continue;
    try {
      await xano.saveModelIntelligenceFinding({
        appliance_model: (f.model || '').toString(),
        appliance_brand: (f.brand || brandHint || '').toString(),
        appliance_category: APPLIANCE_DISPLAY,
        finding_type: (f.finding_type || 'common_failure').toString(),
        source_url: '',
        source_name: SOURCE_DISPLAY,
        title: (f.title || '').toString().slice(0, 200),
        summary: f.summary.toString().slice(0, 2000),
        relevance_score: Number(f.relevance_score || 50),
        scraped_at_ms: Date.now(),
        scout_agent: AGENT_ID,
      });
      saved++;
    } catch (e) {
      log('scout_save_error', { agent_id: AGENT_ID, error: e.message });
    }
  }

  await xano.emitSignal({
    signal_type: 'SCOUT_RUN_COMPLETE',
    signal_strength: 50,
    payload: {
      source: SOURCE_SLUG,
      appliance: APPLIANCE_SLUG,
      findings_saved: saved,
      agent_id: AGENT_ID,
      generated_at_ms: Date.now(),
    },
  });

  const meta = {
    source: SOURCE_SLUG,
    appliance: APPLIANCE_SLUG,
    agent_id: AGENT_ID,
    findings_saved: saved,
    outcome: 'scout_run_complete',
  };
  await xano.markSignalProcessed(signal.id, 'scout_run_complete', meta);
  log('scout_run_complete', meta);

  return { success: true, action: 'scout_run_complete', findings_saved: saved, agent_id: AGENT_ID };
}
