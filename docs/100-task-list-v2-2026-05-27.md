# 100-Task List V2 — Generated 2026-05-27

Next-session paste-ready. Builds on yesterday's session (~46 substantive shipments + 30+ architect-built agents). Themed by impact: REVENUE first, then ELIMINATE-MANUAL-WORK, then COMPLETE-PLATFORM, then INTELLIGENCE, then OPS-HYGIENE.

Each task is sized to ship in <30 min. Many chain to existing agents/endpoints from yesterday's session.

---

## A. REVENUE GENERATION (1-25)

1. **Stripe payment-link auto-send on self-pay completion.** When `tech_job_complete_POST` fires for `customer_type=self_pay`, generate Stripe link + SMS customer. Wire `stripe-webhook.js` back to `tech_earnings.commission_earned`.
2. **Same-day-slot auto-offer agent.** When tech taps Complete >2h before next scheduled job, scan AHS-backlog for nearby zips + SMS Teddy "fill Lee's 2-4pm gap with job #X?"
3. **Pre-appointment upsell SMS** at appointment_start - 24h: "while {tech_first} is there, want a 27-point appliance audit for $99?"
4. **Recurring maintenance reminder agent** — 6 months post-completion, SMS customer: "It's been 6 months since we fixed your {appliance}. Want a check-up?"
5. **Service agreement enrollment offer** — post-completion email/SMS with terms link.
6. **Customer referral tracker** — generate unique referral code per customer + credit at close.
7. **AHS backlog drainer** with operator-set per-day cap (default 50). Rate-limited to avoid SMS flood.
8. **Multi-appliance upsell** at intake — "we noticed you mentioned a fridge, anything else acting up?"
9. **Reactivation campaign** — customers from >2 years ago with no recent job, SMS "we're still here, want a tune-up?"
10. **AHS denial retry agent** — when AHS denies a claim, auto-resubmit with extra documentation.
11. **Senior-discount auto-apply** at intake when DOB on file shows age 65+.
12. **Quote-on-demand endpoint + page** — customer uploads pic + symptoms, Claude returns rough estimate.
13. **Tech tip-jar SMS post-completion** — Venmo/Zelle deeplink in completion SMS.
14. **Bundled appliance audit** — after one repair, suggest adding others in same visit (next visit discount).
15. **Property-manager B2B onboarding** — auth tier with bulk-discount + invoice (not credit card).
16. **Estate appliance buyback** — customer offers used appliance, Ant evaluates + offers cash.
17. **Loyalty tier** — 3+ completed jobs = priority slot access.
18. **Tech-driven upsell tracking** — `jobs.addons_sold_count` + commission share via existing `addon_commission` field on `tech_earnings`.
19. **Cross-sell renewal warranty** when AHS denies coverage — "your home warranty didn't cover this; want a 1-year service warranty from us?"
20. **First-responder verification + discount** auto-apply at intake.
21. **Surge pricing for same-day** — emergency requests get $50 premium auto-added.
22. **Pre-payment for diagnostic fee** — $99 collected via Stripe before truck rolls for non-warranty.
23. **Tech-of-the-month bonus** — leaderboard top spot gets $X bonus auto-credited.
24. **Estimator pricing engine** — endpoint that returns ballpark $ for {appliance, brand, problem}.
25. **Marcone/Triple S API integration** when delivered — replace stopgap Sears Parts Direct link in `teddy-tdr-tool.html`. Refactor `parts_intelligence` template to call the real APIs.

## B. ELIMINATE MANUAL WORK (26-50)

26. **HCP migration cutover Saturday.** Run `colony-loop/scripts/hcp-migration-import.js --max=1 --dry-run` first. Then full run. Cut DNS. Decommission HCP webhook.
27. **Wire vanity numbers to Vapi** — `1-888-ANT-8998` + `1-866-ANT-0111` need Vapi inbound config.
28. **Tech onboarding wizard** — new hire fills form → system creates technicians row, sets PIN, registers in service_zone, sends welcome SMS pack.
29. **TDR auto-fill from tech-ant chat transcript.** Claude extracts {diagnosis, failure_cause, failed_component, labor_hours, repair_completed} from the chat history and pre-fills the TDR form.
30. **Warranty paperwork PDF generator** — auto-fill AHS / Frontdoor / Choice forms from job + TDR + customer data. Danielle just reviews + submits.
31. **Stripe webhook → tech_earnings.commission_earned backfill.** Per `docs/financial-flags-open.md` item #2: it's been $0 for every row.
32. **Tax 1099 generator** — quarterly aggregate per tech, paid_at-bucketed.
33. **Payroll-batch UI trigger button** — `docs/financial-flags-open.md` item #6.
34. **Vapi inbound voice agent** — answer + qualify + create job (after-hours fallback).
35. **Auto-deny out-of-area AHS jobs** — when intake zip not in service_zone with accept_new_jobs=true, auto-bounce to AHS with decline reason.
36. **Auto-cancel ghost intake** — jobs in `not_ready` >14 days with no customer touch get auto-canceled + audit logged.
37. **Auto-archive completed jobs** — >90 days completed → flag for cold-storage table (don't actually move yet; just flag).
38. **Receipt OCR for parts costs** — tech photos receipt → Claude extracts part numbers + cost → auto-fills TDR.
39. **Email-intake adapter generator** — pattern that lets new vendor emails (Choice, etc) be parsed without code changes — just config.
40. **Auto-create HCP appointment from Ant-source jobs** (legacy bridge until cutover).
41. **Auto-translate Spanish AHS dispatches** — Claude pre-translates inbound Spanish into English for office.
42. **Real-time customer dedup at intake** — match by phone or normalized address → merge instead of double-record.
43. **Auto-categorize event_log severity** — INFO / WARN / ERROR taxonomy for office-pulse filtering.
44. **Daily AR aging email to Alyse** — replace BI agent SMS path with HTML email.
45. **Auto-generate SEO landing pages** per service-zone zip (template-driven).
46. **Auto-weekly blog post** — Claude generates a "[Brand] [Appliance] common failures" article weekly.
47. **Auto-flag suspicious reviews** — sentiment + IP/device fingerprint analysis.
48. **License-expiration auto-alert** — 60/30/7 days before tech's license expires.
49. **Auto-suspend tech for compliance violation** — license expired, insurance lapsed, etc → block job assignment.
50. **Weekly Teddy email digest** — long-form HTML version of daily SMS, includes charts.

## C. COMPLETE THE PLATFORM (51-75)

51. **PWA wrap of tech-daily-dashboard** — installable app on tech phones with offline cache for today's jobs.
52. **PWA wrap of customer-portal** — installable for customers.
53. **Push notifications via web-push** — replace SMS for tech direction (saves Telnyx $).
54. **Tech in-truck knowledge base** — searchable repair guides indexed by brand+model+symptom.
55. **Tech training video library** integrated in tech-ant-live (per-appliance index).
56. **Vehicle maintenance tracking** — per-tech mileage + oil + tire dates.
57. **Tool inventory tracking** — per-tech tool list with replacement reminders.
58. **Multi-language support** — Spanish translation pipeline for LA customer-facing SMS.
59. **Insurance verification automation** — homeowner vs renter at intake.
60. **Customer photo gallery** in customer-portal — view all job photos.
61. **Tech-to-tech messaging channel** within Ant (replaces group text).
62. **Office-to-tech broadcast announcements** — one-click "snow day, all techs off".
63. **Per-tech profile photo** — show on customer-portal "your tech today" card.
64. **Job-completion-photo requirement** — server gate on `tech_job_complete` requires at least 1 attachment.
65. **Public customer reviews on appliance-ant.html** landing page (filtered to ≥4 stars).
66. **Real-time tech location tracking** (opt-in) — show on office-calendar.
67. **TV-display dashboard** — kiosk mode for office waiting room screen.
68. **Quality-assurance audit flow** — senior tech reviews random completed jobs.
69. **Two-factor auth for office pages** — TOTP via authenticator app.
70. **Per-user office accounts** — replace shared password with named accounts.
71. **API key rotation UI** — office can rotate keys safely without code change.
72. **Calendar week-print view** — paper backup for power-outage scenarios.
73. **Andre Personal Brand colony** — populate the empty colony with content-generation agents.
74. **Story and Why colony** — populate the empty colony with narrative-content agents.
75. **Vendor portal automation** — Choice, Frontdoor, ServicePower web-portal scraping/submission.

## D. INTELLIGENCE & ANALYTICS (76-90)

76. **Customer churn prediction model** — Claude classifies churn risk per customer based on visit history.
77. **Demand forecasting** — predict next week's volume by zip from historicals.
78. **Tech burnout detector** — flag when tech's completion rate or rating trend declines >30%.
79. **Service-zone profitability heatmap** — endpoint + page showing per-zip margin.
80. **Most-common-failure-mode dashboard** per appliance (uses the `tdr_catalog_record` data shipped previously).
81. **Parts-cost trend chart** per supplier — track inflation/discount.
82. **Wire CI002 LTV calculator to invoice flow** — fire on every completion (already partial — done yesterday).
83. **Job-completion-time outlier detector** — too fast = sloppy, too slow = overbilling risk.
84. **Tech-vs-tech callback comparison** — surface in tech-performance.
85. **Margin analyzer** — AHS vs Frontdoor vs self-pay $/hour comparison.
86. **Peak-hours load chart** — visualize daily/weekly inbound volume.
87. **Cancellation reason taxonomy** — Claude classifies free-text cancel reasons into 5-10 buckets.
88. **First-visit-fix-rate trend by appliance type** — find which appliances we're weakest on.
89. **Customer geographic cluster map** — visualize density on a map.
90. **Lifetime appliance failure prediction** — given install date + brand + model, predict next failure window.

## E. OPS HYGIENE & ROBUSTNESS (91-100)

91. **Mac Mini hardware replacement playbook** — operator-readable, copy-paste recovery steps.
92. **Loop process supervisor beyond launchd** — auto-restart on slow tick (>30s) or stuck heartbeat.
93. **Xano backup verification job** — weekly test-restore from latest snapshot.
94. **S3 backup uploader activation** — set BACKUP_S3_BUCKET env + IAM policy + verify upload.
95. **Telnyx delivery-receipt webhook + retry** — track delivered vs failed SMS, retry failures.
96. **Cross-region Mac Mini hot standby** — second Mac in different location, periodic state sync.
97. **Signal-processing latency spike alert** — page Teddy when median tick_ms > threshold.
98. **Agent crash-loop detector** — page when same signal_type errors >5 times in a row.
99. **Operational runbook auto-generator** from the live loop config + plist files.
100. **Quarterly DR drill** — scripted full simulated outage + verify recovery RTO < 2 hours.

---

## How to use this list (paste at top of next session)

```
Read CLAUDE.md. Build through docs/100-task-list-v2-2026-05-27.md.

Rules:
- Push after every task (single commit per task).
- Inject COLONY_ARCHITECT max_builds=999 after every 10 tasks.
- Skip a task only if it's already shipped (check git log first); annotate skip in commit message and move on.
- For Stripe / Vapi / 3rd-party tasks, build the integration scaffold even if env keys are missing — leave the operator a one-line config note in the commit message.
- For tasks requiring schema changes (e.g. tech birthday), use Metadata API column-add via xano CLI. Never PUT/PATCH the schema endpoint.
- Verify each shipment with a curl smoke-test before commit. Log endpoint + result in the commit message.
- Watch for the xs-footguns documented in docs/xanoscript-footguns.md (no em-dashes, no try/catch, no backtick template literals, single-line ternaries, no |trim in if(), no json_decode without surrounding bind).
- When `expand-blueprint-N.js` opportunity arises (e.g. populating empty colonies), batch it.

Priorities in order:
1. Tasks 1-10 (revenue-direct, high $/build)
2. Tasks 26-30 (HCP cutover blockers + biggest manual-work eliminators)
3. Then proceed in numerical order within each section, sweeping A → B → C → D → E.
4. If architect runs idle (no TO_BUILD), pause to add a template — see colony-loop/architect/templates.js.
5. End the session with COLONY_ARCHITECT injected + a CLAUDE.md session log update.

Long Live Ant. 🐜
```
