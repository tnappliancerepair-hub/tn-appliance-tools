# 100-Task List V3 — 2026-05-27 overnight

Tilted toward the three strategic moves Teddy + I agreed on:
- **A. SaaS multi-tenant foundation** (1-30) — every endpoint scopes to company_id, settings table, per-tenant SMS/Stripe/branding, onboarding flow
- **B. Vector store + intelligence** (31-55) — pgvector (if Xano supports) or scaffold, ingestion pipeline, ask_ant v2 semantic retrieval, multi-agent collab, closed-loop training data
- **C. Security hardening** (56-80) — kill office-password-in-HTML, per-user accounts, 2FA, rate limiting, PII masking in event_log, webhook signature verification
- **D. Tech confidence + revenue ride-alongs** (81-100) — quick wins that polish the morning demo + add net revenue surface

Format: tight enough to ship in a 30-min slot. Skip-as-already-done is OK; annotate in commit msg.

---

## A. SaaS Multi-Tenant Foundation (1-30)

1. `company` table via Metadata API: id, name, slug, owner_phone, owner_email, status, created_at
2. `company_settings` table: company_id, key, value (key/value flexibility for branding, telnyx_from, timezone, etc)
3. Default company row: `company_id=1, slug=tn, name=TN Appliance Exchange LLC`
4. Add `company_id` column (default 1) to: customer
5. Add `company_id` to: jobs
6. Add `company_id` to: technicians
7. Add `company_id` to: tech_earnings
8. Add `company_id` to: technician_decision_report
9. Add `company_id` to: tech_assist_session
10. Add `company_id` to: event_log (high-cost; consider deferred)
11. `get_company_settings_GET` endpoint: returns settings for company_id with fallback to defaults
12. `set_company_setting_POST` endpoint: upserts (company_id, key, value)
13. `colony-loop/config.js` — add `companyId` (env COMPANY_ID, default 1) for the local loop's anchor company
14. `colony-loop/xano.js` — wire all GET-fired endpoints to pass company_id
15. `sms.js` — read recipient-direction Telnyx FROM number per-tenant from company_settings
16. `onboard_company_POST` endpoint: takes {name, slug, owner_phone, owner_email}, creates company + initial settings + sends welcome SMS
17. `signup.html` public page: form to create a new company (free trial gate)
18. `company-admin.html` per-tenant admin dashboard (settings, techs, billing)
19. Stripe per-tenant subscription: create_subscription Netlify function
20. `subscription_status_POST` endpoint: webhook receiver for Stripe subscription events
21. `get_active_techs_count_GET` per company (drives per-tech billing)
22. Per-tenant timezone: derive from settings, drop CT hardcode in date math (medium effort — many tick.js + agent files)
23. Per-tenant owner_phone: `toOwner` reads from settings not config
24. Per-tenant danielle_phone: `toDanielle` reads from settings (or future generalize to "warranty_handler_phone")
25. Per-tenant `publicSiteBase`: settings-driven so other companies get their own subdomain
26. Tenant-aware office-pulse / office-todo (filter by company_id)
27. Tenant-aware health-check (per-company heartbeat tracking)
28. `cross_tenant_parts_catalog_GET` (anonymized aggregation across all companies)
29. `cross_tenant_failure_modes_GET` (anonymized common-failures per appliance+brand)
30. Multi-tenant migration plan doc: `docs/multi-tenant-migration.md`

## B. Vector Store + Intelligence (31-55)

31. Check if Xano supports pgvector — if so, design `embeddings` table
32. If pgvector unavailable: scaffold Pinecone integration via Netlify function (operator sets PINECONE_API_KEY + PINECONE_INDEX)
33. `embed_text` Netlify function: takes text + namespace, returns embedding (uses Anthropic? or OpenAI text-embedding-3-small via OPENAI_API_KEY)
34. Backfill embeddings job: walk technician_decision_report + index each
35. Backfill: walk customer.notes, index
36. Backfill: walk tech_assist_session_messages, index
37. On-event ingestion: when create_tdr fires, also index it
38. On-event ingestion: when tech_assist_chat message lands, index it
39. `ask_ant_v2_POST` endpoint: semantic retrieval, returns top-N matches with source links
40. Update `ask_ant.html` UI: search bar + cited results
41. `find_similar_jobs_POST`: given a job_id, return semantically similar historical jobs
42. Wire `find_similar_jobs` into tech-ant-live as "Similar jobs" quick-action
43. Multi-agent collab v1: tech_assist_chat appends top-3 similar-jobs context to Claude prompt
44. Multi-agent collab v2: chain brand_lookup_<brand> + parts_lookup_<best supplier> outputs into TDR draft
45. `claude_call_log` table: input + output + model + cost + downstream outcome (for closed-loop reinforcement)
46. Endpoint that pulls top-performing prompts per (agent, scope) by win-rate
47. Predictive failure scaffold: `predict_next_failure_GET?appliance=fridge&brand=lg&model=X&install_year=2020`
48. Train data prep script: walk completed TDRs → (brand, model, install_year, failure_mode, days_since_install)
49. Simple statistical model: median/p90 days-to-failure per (brand+model) bucket
50. SMS agent: when high-confidence prediction window opens, SMS customer "your unit is in its typical failure window — want a check-up?"
51. Per-tech recommendation engine: surface "you might want to check X" based on prior similar jobs
52. Customer-portal "similar customer success" social proof block
53. Tech-ant-live "history" panel: prior jobs for this exact customer's appliance
54. Knowledge-base entry generator: pull top-frequency failure modes + Claude-write playbooks
55. Indexing dashboard: how many TDRs/messages embedded, ingestion lag, embedding spend

## C. Security Hardening (56-80)

56. **CRITICAL** server-side `verify_office_password_POST` endpoint (replace plaintext in HTML)
57. Update office-calendar / office-pulse / office-todo / customer-search / customer-feedback / office.html / payroll / tech-onboard / dispatch-tv / warranty-review to use server auth
58. `office_user` table: id, company_id, email, password_hash, role, created_at, last_login_at
59. `office_signin_POST`: bcrypt verify, returns short-lived JWT (Xano supports JWT)
60. `office_signout_POST`: revokes session
61. Per-user audit: `office_audit_log` table for sensitive actions
62. 2FA TOTP: `enable_2fa_POST` + `verify_2fa_POST`
63. PII masking helper: `phone_last4_only` filter applied to event_log metadata writes
64. Update event_log writers across the codebase to use the masking helper
65. Rate-limit endpoint: `check_rate_limit_GET` per (ip, endpoint, window)
66. Apply rate limit to quote, customer-portal-action, generate_quote, record_inbound_call
67. Telnyx voice webhook HMAC signature verification (vapi-webhook.js)
68. Anthropic API key rotation runbook
69. `.env` secret-scan script (warn on committed secrets)
70. CORS lockdown on Netlify functions (allowlist tnapplianceexchange.net only)
71. CSP headers on HTML (Netlify config)
72. Input length caps everywhere customer text is accepted
73. SQL-injection audit: confirm Xano abstracts everything (it does — no raw SQL exposed)
74. Dependency upgrade pass: `npm audit --json` in netlify/ + any package dirs
75. Mac Mini physical-access hardening doc: FileVault on, SSH off, screen lock
76. Backup-restore drill (run xano-backup → simulate restore from latest snapshot)
77. Secrets vault recommendation: 1Password or AWS Secrets Manager
78. Production-vs-dev environment split: ensure xano dev workspace exists for testing
79. Webhook replay-attack prevention (nonce + timestamp check on inbound webhooks)
80. Security disclosure policy doc: `docs/security.md` for responsible disclosure

## D. Tech Confidence + Revenue (81-100)

81. Tech-ant-live: "Show prior visits" panel pulls customer's history
82. Tech-ant-live: "Common failures" panel for this appliance/brand from cross-tenant catalog
83. Tech-ant-live: voice-to-text input (browser SpeechRecognition is already wired — verify it works)
84. Tech-ant-live: large "Mark on the way" sticky button on first load if not yet on the way
85. Tech-daily-dashboard: add "next stop" countdown when current job > 1h running
86. Customer-portal: "share with spouse" link generator (sends same portal URL to a 2nd phone)
87. Customer-portal: appointment.ics download (Apple Calendar / Google integration)
88. Office-calendar: drag-to-reschedule (instead of click-modal)
89. Office-calendar: keyboard shortcuts (J/K for next/prev day, R for refresh)
90. Teddy-tdr-tool: bulk pre-diagnosis (queue 10 jobs, Claude triages each)
91. Teddy-tdr-tool: "copy diagnosis to chat" button to seed Ant for the tech
92. Inbound SMS auto-router: keywords route to correct agent (already partial — verify)
93. Outbound SMS: link-shortener for cleaner customer SMS (Bitly or self-hosted)
94. Tech leaderboard: weekly winner SMS to team channel
95. Office-pulse: filter chips for tech_id, customer_id, signal_type
96. New-vehicle/tool inventory pages from knowledge-base-setup doc
97. Warranty review: 1-click resubmit with denial-addressed notes
98. Self-pay quote → booking conversion flow
99. Multi-language SMS templates (Spanish for LA)
100. Final session log + COLONY_ARCHITECT inject + sleep

---

## Cadence

Same as V1+V2:
- Push after every task
- Inject COLONY_ARCHITECT max_builds=999 every 10 tasks
- Skip-as-already-done is OK; note in commit msg
- Watch XS footguns (no em-dashes, no try/catch, no find-on-not-found, no |trim in if())
- Verify with curl smoke-test before commit
- End with session log + final architect

🐜 Long Live Ant.
